---
title: 'Fix the scan --json brief-pool jq (.specs -> .briefs, real CI bug) + close-job via ''spec'' -> ''brief'''
slug: fix-scan-json-brief-pool-jq-and-close-job-via
brief: code-identifier-slice-prd-to-task-brief-rename
blockedBy: []
covers: []
---

> **AUTHORED 2026-06-23 (conductor + human, post-rename cleanup).** Closes two verified defects the rename drive surfaced as `needsAnswers` observations (`scan-json-specs-key-vs-jq-path-possible-mismatch.md`, `close-job-via-spec-code-literal-vs-renamed-brief-field.md`). The first is a REAL silent CI bug.

## What to build

Two independent, mechanical fixes in one small task (both are the `.specs`/`'spec'` literal lagging the already-renamed `briefs`/`brief:` reality):

### Fix 1 — the `scan --json` brief-pool `jq` key (REAL BUG)

`scan --json` serialises the taskable-brief pool under the key **`briefs`** (`scan.ts` `RepoReport.briefs: ScannedBrief[]`, line ~169 — there is NO `specs` key/alias). But the emitted advance CI matrix `jq` reads `.repos[].specs[]?` + `.cwd.repo.specs[]?`, so it matches NOTHING → the propose matrix enumerates ZERO briefs on the cron (capability B silently dead — the exact failure `ci-propose-matrix-must-enumerate-sliceable-prds-not-only-slices` guards against, inverted by the field rename).

Rename `.specs[]` → `.briefs[]` (and `repos[].specs[]`/`cwd.repo.specs[]` → `...briefs[]`) in:

- `packages/dorfl/src/advance-lifecycle-template.ts` — the emitted `jq` (line ~314: `(.repos[].specs[]?, .cwd.repo.specs[]?)`) + the validator regex (`/\.specs\[\]/`, ~line 690) + the comments naming `specs[]`.
- `packages/dorfl/src/advance-ci-template.ts` — the validator regex (`/\.specs\[\]/`, ~line 142) + the comments.
- `packages/dorfl/src/mirror-pool-scan.ts` — any `.specs` reference.
- The snapshot/assert tests: `advance-ci-template.test.ts` (asserts `\.repos\[\]\.specs\[\]\?`), `advance-lifecycle-template.test.ts`, and `scan.test.ts` if it asserts the key — update to `.briefs[]` / `briefs`.

This edits the template EMITTER source (NOT `.github/workflows/*` — those are regenerated by a human via `install-ci`; agents never touch workflow files). Confirm the emitted `task:`/`brief:`/`obs:` legs are otherwise unchanged.

### Fix 2 — `close-job.ts` `via: 'spec'` -> `via: 'brief'`

`close-job.ts` uses a local discriminator `via: 'issue' | 'spec'` (the type at ~L74/L152, the object literals `via: 'spec'` at ~L156/L173, `closeComment(via: 'issue' | 'spec', ...)` at ~L217). The concept it discriminates is a BRIEF (its query reads the `brief:` frontmatter; `resolveClosingIssue` in `frontmatter.ts` already returns `via: 'brief' | 'issue'`). Rename the close-job discriminator `'spec'` → `'brief'` end-to-end (type, literals, the `cand.via === 'spec'` branch if any, `closeComment`) so it matches the `brief:` field it reads. Update the close-job test asserting `via: 'spec'`.

## Acceptance criteria

- [ ] The emitted advance CI matrix `jq` reads `.repos[].briefs[]?` + `.cwd.repo.briefs[]?` (NOT `.specs[]`); the validators + the `advance-ci-template`/`advance-lifecycle-template` snapshot tests assert `.briefs[]`; no `.specs[]` remains in the emitters.
- [ ] `close-job.ts` discriminator is `via: 'issue' | 'brief'` (no `'spec'` literal); it aligns with `resolveClosingIssue`'s `via: 'brief'`; its test is updated.
- [ ] No `.github/workflows/*` file is edited (emitter source only).
- [ ] `pnpm -r build && pnpm -r test && pnpm format:check` green.

## Blocked by

- None — startable immediately. Independent of the other cleanup tasks (different files).

## Prompt

> Goal: fix two verified `.specs`/`'spec'`-literal-lagging-`briefs`/`brief:` defects, per brief `code-identifier-slice-prd-to-task-brief-rename`. Fix 1 is a REAL silent CI bug (the propose matrix enumerates zero briefs because the emitted `jq` reads a `.specs[]` key that `scan --json` does not emit — it emits `.briefs[]`).
>
> FIRST verify reality: confirm `scan.ts` serialises the brief pool as `briefs` (RepoReport.briefs, no `specs` alias), and that the emitters' `jq` + validators still read `.specs[]`. Confirm `close-job.ts` still uses `via: 'spec'` while `frontmatter.ts` `resolveClosingIssue` returns `via: 'brief'`.
>
> Where to look: `advance-lifecycle-template.ts` (emitted `jq` + validator), `advance-ci-template.ts` (validator), `mirror-pool-scan.ts`, the matching `*.test.ts`; and `close-job.ts` (the `via` type/literals/closeComment) + its test. Edit the template EMITTERS, never `.github/workflows/*`. Run `pnpm format`.
>
> Done = build/test/format:check green, the emitted matrix `jq` reads `.briefs[]`, close-job `via` is `'brief'`, no workflow file touched.

---

### Claiming this task

```sh
dorfl claim fix-scan-json-brief-pool-jq-and-close-job-via --arbiter <remote>
git fetch <remote> && git switch -c work/fix-scan-json-brief-pool-jq-and-close-job-via <remote>/main
git mv work/tasks/todo/fix-scan-json-brief-pool-jq-and-close-job-via.md work/tasks/done/fix-scan-json-brief-pool-jq-and-close-job-via.md
```

---

## Update 2026-06-25 (backwards-premised: this record's fixes describe a REGRESSION)

This task was authored with the rename direction BACKWARDS. `spec` is the NEW,
current vocabulary; `brief` is the RETIRED old word (confirmed by the human).
Both "fixes" therefore demanded renaming current vocabulary back to the retired
word, and were correctly never applied:

- **Fix 1** (`scan --json` key `.specs[]` -> `.briefs[]`): NOT a bug. On `main`
  `scan.ts` emits `specs: ScannedPrd[]` and the emitted matrix `jq` reads
  `.repos[].specs[]?` + `.cwd.repo.specs[]?`, and they MATCH on the new word `spec`.
  The premise "RepoReport TS field is now `briefs`" is false.
- **Fix 2** (`close-job.ts` `via: 'spec'` -> `via: 'brief'`): NOT stale.
  `close-job.ts` (`via: 'issue' | 'spec'`, the `via: 'spec'` literals, the
  `cand.via === 'spec'` branch, `closeComment`) and `frontmatter.ts`
  `resolveClosingIssue` (returns `{via: 'spec'; spec}` and reads `frontmatter.spec`)
  are CORRECT current vocabulary. The test asserting `.via).toBe('spec')` is right.

So this record's done-criteria (`.briefs[]`, `via: 'issue' | 'brief'`) describe a
regression and should not be treated as outstanding work. The two observations it
claimed to close were themselves backwards-premised and have been discharged via
`dorfl drop` (`close-job-via-spec-code-literal-vs-renamed-brief-field`,
`scan-json-specs-key-vs-jq-path-possible-mismatch`). Kept as a record (not
deleted) because it is a `done/` artifact; this update is the correction trail.
Source: sidecar rebuild sweep finding B, corrected by the human's note that
`spec` is the new word.
