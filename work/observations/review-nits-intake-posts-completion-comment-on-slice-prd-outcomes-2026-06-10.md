---
title: review-gate non-blocking nits for 'intake-posts-completion-comment-on-slice-prd-outcomes' (Gate 2 approve)
date: 2026-06-10
status: open
slug: intake-posts-completion-comment-on-slice-prd-outcomes
---

## Non-blocking review findings

The PR/code review gate (Gate 2) APPROVED 'intake-posts-completion-comment-on-slice-prd-outcomes' but raised the following non-blocking findings (nits). They do not block integration; this is their durable home for triage — promote-to-slice / keep / delete.

- Ratify the extra user-visible sentence the completion comment asserts: "This is an informational update — the issue stays open (it remains in play until the slice/PRD lands; intake does not change the issue's state)." Is that the wording you want on every productive outcome? (The slice specified the comment be framed as `slice created`/`prd created`, never `resolved`, and that it changes no state — which the body satisfies. But the agent additionally added an explicit "the issue stays open / remains in play until the slice lands" clause that the slice did not enumerate. It is a user-visible default on a comment maintainers and issue authors will read on every intake success. It is coherent (and arguably helpful — it pre-empts the "why is this still open?" question), but it is an in-scope wording decision the agent made on its own, worth a nod. Note also "remains in play until the slice lands" subtly implies the issue WILL be closed when the slice lands, which is true via CI's future close-job but couples the comment's promise to a not-yet-built job.)
- Update the stale JSDoc on `IntakeResult.commented`, which still reads "True iff a comment was posted on the issue (ask / bounce outcomes)." — it is now ALSO set on slice/prd via the completion comment. (In intake.ts the `commented?` field comment was not updated when this slice began setting it on the slice/prd success path (`commented: posted`). The behaviour is correct and tested (degrade flips it to false), but the doc now under-describes the field's meaning. Minor, non-blocking; flagging so the contract documentation stays honest for the CI/consumers that read `commented`. (Related, already-captured-on-base: `commented` is also re-used on the bounce path to mean "closing comment landed" — see the prior slice's nit; the field now spans ask, bounce-close, and slice/prd-completion, so a one-line clarification covering all three would help.))
- Ratify the whitespace-only reformat of the unrelated base-branch file `work/observations/review-nits-intake-closes-issue-on-bounce-2026-06-10.md` pulled into this slice's diff, and consider the proposed standalone 'format the base observations' chore. (The base branch carries observation files committed UNFORMATTED that are RED on `format:check` independent of this slice. Running `pnpm format` (the repo's documented fix step) to keep the gate green rewrapped that base file (pure line-wrap, no content change). The agent honestly recorded the recurrence in `preexisting-unformatted-observation-file-fails-format-check-2026-06-10.md`, noting this is now the THIRD slice to inherit the cross-touch and proposing a one-off chore to format the base and stop every future slice inheriting it. Content-neutral and well-documented; flagging only so the human ratifies the cross-touch (vs treating it as scope creep) and weighs the proposed chore.)
