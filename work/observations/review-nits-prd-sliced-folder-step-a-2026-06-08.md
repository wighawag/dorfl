---
title: review-gate non-blocking nits for 'prd-sliced-folder-step-a' (Gate 2 approve)
date: 2026-06-08
status: open
slug: prd-sliced-folder-step-a
---

## Non-blocking review findings

The PR/code review gate (Gate 2) APPROVED 'prd-sliced-folder-step-a' but raised the following non-blocking findings (nits). They do not block integration; this is their durable home for batch-qa triage (promote-to-slice / keep / delete).

- Ratify the in-scope decision: backfill is shipped as an EXPORTED one-shot library function (backfillSlicedPrds) that the CALLER must invoke + commit, rather than being auto-wired into setup/scaffold or a runtime path. The slice left this 'one-shot vs setup-time' choice open. Is a manually-invoked, caller-owns-the-commit migration the intended ergonomics, or should it be wired into a setup/scaffold step so an existing repo self-heals without a human running it? (src/prd-sliced-migration.ts exports backfillSlicedPrds and index.ts re-exports it; grep shows NO production caller (only the test + the dist .d.ts). The function does git mv but does NOT commit/push (doc-comment: 'the caller owns the commit'). In THIS repo the backfill was applied directly in the diff (8 PRDs renamed), so this repo is migrated — the question is the general ergonomics for other repos.)
- Ratify the in-scope decision: the slicing-lock.ts release branch that handles emitSlices/markSliced still moves the PRD slicing/ -> prd/ and writes the sliced: marker there (the OLD destination), and is now UNREACHABLE on the success path (performSlice routes success through stageSlicingLifecycle -> prd-sliced/ and never passes emitSlices/markSliced to release). It is left in place because the slice's scope fence said 'do NOT touch the slicing LOCK semantics'. Confirm this dead-but-wrong-destination branch is intended to be cleaned up by Step B / a follow-up rather than now. (src/slicing-lock.ts releaseAttempt lines ~720-748: the else branch git mv slicing -> prd then setSlicedMarker on prd, gated on markSliced/emitSlices. grep confirms no caller passes those params to releaseSlicingLock anymore — the only emitSlices: reference outside slicing-lock.ts is stageSlicingLifecycle's own param. Latent, not live; flagging so it is owned, not forgotten.)
- Stale doc-comments now misdescribe sliced-ness as 'the sliced: marker' in files whose behaviour did not change. The slice's acceptance criterion aimed at 'no reference still claiming the sliced: marker resolves sliceAfter' — these are comment-level survivors. Worth a sweep (now or Step B) so the next author is not misled. (src/slicing-eligibility.ts line 6 ('resolved against the `sliced:` marker') and line 33 ('their `sliced:` marker is set'); src/select-priority.ts line 71 ('whose PRD already carries a `sliced:` marker'). These files correctly consume only the slicedSlugs Set; the Set is now folder-derived, so the comments describing HOW it is populated are stale. No runtime impact.)
