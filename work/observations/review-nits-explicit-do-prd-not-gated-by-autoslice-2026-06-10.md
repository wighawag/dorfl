---
title: review-gate non-blocking nits for 'explicit-do-prd-not-gated-by-autoslice' (Gate 2 approve)
date: 2026-06-10
status: open
slug: explicit-do-prd-not-gated-by-autoslice
---

## Non-blocking review findings

The PR/code review gate (Gate 2) APPROVED 'explicit-do-prd-not-gated-by-autoslice' but raised the following non-blocking findings (nits). They do not block integration; this is their durable home for triage — promote-to-slice / keep / delete.

- Ratify the chosen seam: the fix adds an `explicit` boolean threaded through PerformSliceOptions → SlicingEligibilityInput → resolveSliceGate (`return explicit || autoSlice`), rather than splitting the policy term out so only the pool caller applies it. Is the added-flag seam the one you want as the durable shape? (The slice prompt explicitly left this open ('Decide the cleanest seam'). The agent chose to add `explicit?: boolean` (default false) and short-circuit the policy term when set. slicing-eligibility.ts:70 `resolveSliceGate(... explicit = false)`; do.ts:475 / do.ts:1273 pass `explicit: true`. Coherent and minimal, but an in-scope design choice the agent made on its own.)
- Ratify that the per-invocation `autoSlice` gate term is now PRODUCTION-DEAD: both production callers of performSlice pass `explicit: true`, so `autoSlice` is enforced only by the auto-pick pool's `sliceablePrds`. The non-explicit gate term and the `autoSlice` branch of `gateRefusalReason` now serve only the pool-default code path and tests. Is keeping the term as a defensive belt (vs removing it / moving the check entirely to the pool, as the finding's 'MOVE the check' wording suggested) the intended end-state? (grep confirms exactly two production performSlice callers (do.ts:462, do.ts:1263), both explicit:true. The pool path (select-priority.ts sliceablePrds) calls resolveSlicingEligibility WITHOUT explicit (defaults false), so it remains the single autoSlice enforcer. work/findings/autoslice-gate-conflates-verb-autonomy-and-review-loop.md §'Concern 1' suggested MOVING the check to the selection step; the landed design instead keeps the term and bypasses it via explicit — functionally equivalent and correct, but a slightly different shape than the finding's letter.)
- Conceptual coherence: the new slicing `explicit` flag ('this PRD was named, policy already satisfied') shares a NAME with the existing `explicit: boolean` in slug-namespace.ts ('an explicit namespace prefix prd:/slice: was typed'). They are related but DISTINCT (a bare `do <slug>` slice is slug-namespace `explicit:false` yet still a named target that bypasses allowAgents). Consider pinning the slicing-gate sense in the CONTEXT.md glossary or renaming (e.g. `named`/`policySatisfied`) so the next author cannot conflate them. (slug-namespace.ts:56,150,161 use `explicit` for prefix-vs-bare resolution; the new flag (slicing-eligibility.ts:46, slicing.ts:153, do.ts:158) uses `explicit` for named-target-vs-auto-pick policy bypass. CONTEXT.md line 78 makes conceptual coherence a first-class, glossary-enforced quality; the new term is not yet in the glossary. No actual code confusion today (the new flag is thoroughly docstring'd), so non-blocking — but it is exactly the kind of same-word-two-meanings drift the coherence lens exists to surface.)
