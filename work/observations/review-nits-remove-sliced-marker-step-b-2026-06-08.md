---
title: review-gate non-blocking nits for 'remove-sliced-marker-step-b' (Gate 2 approve)
date: 2026-06-08
status: open
slug: remove-sliced-marker-step-b
---

## Non-blocking review findings

The PR/code review gate (Gate 2) APPROVED 'remove-sliced-marker-step-b' but raised the following non-blocking findings (nits). They do not block integration; this is their durable home for batch-qa triage (promote-to-slice / keep / delete).

- Ratify deleting prd-sliced-migration.ts (backfillSlicedPrds) + its test outright in Step B — was that the intended disposition, or should the one-shot backfill have survived for not-yet-migrated downstream repos? (The slice named the frontmatter type/parser/plumbing and PRD files for the marker removal but did NOT explicitly name prd-sliced-migration.ts for deletion. The agent deleted it (and its test, and the index.ts re-exports of backfillSlicedPrds/BackfilledPrd/BackfillResult). This is sound and arguably necessary: the function's core predicate was `if (fm.sliced === undefined) continue`, and fm.sliced no longer exists as a parsed field — so post-Step-B the backfill would always see undefined and move nothing, i.e. it is dead. A Step-A review observation (review-nits-prd-sliced-folder-step-a-2026-06-08.md) had already flagged the 'one-shot library vs setup/scaffold auto-wire' ergonomics question for this migration as unratified; deleting it now closes that question by removing the tool. If other repos still carry sliced: PRDs that were never folder-migrated, the only migration path is gone (they'd need a manual git mv). Confirm that's acceptable (this repo IS migrated — the 8 PRDs were stripped + already reside in prd-sliced/).)
- Ratify leaving the now-doubly-stale emitSlices branch in slicing-lock.ts (releaseAttempt) — it still restores slicing/ -> prd/ (the OLD destination, NOT prd-sliced/) and is dead on the success path. Defer its cleanup to a follow-up, or is leaving a wrong-destination latent branch a trap? (Step A's review observation explicitly asked to 'confirm this dead-but-wrong-destination branch is intended to be cleaned up by Step B / a follow-up rather than now.' Step B removed the markSliced half of it (the marker write) but left the emitSlices half, which still does `git mv slicing -> prd` (work/prd/, not work/prd-sliced/). I verified it is LATENT: in slicing.ts, emitSlices is only ever passed to stageSlicingLifecycle (the performIntegration path → prd-sliced/), NEVER to releaseSlicingLock — so this branch has no live caller. Leaving it is defensible under Step B's inherited scope fence ('do NOT touch the slicing LOCK semantics'), which is why this is non-blocking. But it now contradicts the new world (residence in prd-sliced/) and is a wrong-destination trap for the next author who wires emitSlices into a release. Human should ratify deferring it (and ideally track a follow-up slice to delete the dead emitSlices release branch) rather than let it rot.)
- Ratify the scope boundary on skills/to-slices/SKILL.md step 6, which STILL instructs adding a `sliced: <YYYY-MM-DD>` line to the PRD frontmatter — now an instruction to write a field that no longer exists. Intended to be coordinated separately in the maintainer's skills tree? (The slice prompt itself drew this boundary: 'the to-slices SKILL describes setting sliced: in its step 6 — coordinate that doc with the maintainer's skills tree; the in-repo WORK-CONTRACT copy is in scope.' The in-repo WORK-CONTRACT.md copy WAS correctly updated ('There is no sliced: frontmatter marker (it was removed in remove-sliced-marker-step-b)'). So this is a deliberate, slice-sanctioned scope exclusion, not a miss — hence non-blocking. Flagging only because SKILL.md line 155 now actively tells a future slicer to stamp a removed marker; the maintainer should fix it in the skills tree (or convert it to 'move the PRD into work/prd-sliced/') so the two copies don't diverge in meaning.)
