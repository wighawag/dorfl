---
title: review-gate non-blocking nits for 'stale-lease-retry-all-push-sites-and-treeless-surface' (Gate 2 approve)
date: 2026-06-12
status: open
slug: stale-lease-retry-all-push-sites-and-treeless-surface
---

## Non-blocking review findings

The PR/code review gate (Gate 2) APPROVED 'stale-lease-retry-all-push-sites-and-treeless-surface' but raised the
following non-blocking findings (nits). They do not block integration; this
is their durable home for triage — promote-to-slice / keep / delete.

- The slice has three acceptance criteria requiring a `## Decisions` block — PIN the current per-site push-failure behaviour BEFORE the change (helper-throw-after-cap vs bare-push non-zero exit; does it surface or sit in-progress?), record the start.ts catch discrimination, and record the tree-less-vs-cwd-bound surface choice. No `## Decisions` block exists in any PR description, artifact, or the slice file. The substance of the discrimination and the strand bug IS captured in inline code comments, but the explicit before-state PIN (the analysis the human ratifies) was not produced as a recorded deliverable. Ratify that the inline comments suffice, or have the agent add the Decisions block to the PR description.
  (AC lines 64, plus the Part-A SAFETY note (line 29) and Part-B (line 39). grep for '## Decisions' finds only the slice's own instructions, no produced block. Code comments in start.ts (isOfflinePushFailure docstring, continueFromKeptBranch try/catch) and do.ts/run.ts (step 4b comments) carry the rationale.)
- Part B took option (b): the surface routes through the EXISTING cwd-bound `applyNeedsAttentionTransition`/`routeToNeedsAttention` (a working tree with HEAD on the work branch in do/run; a temp-branch-off-main checkout in start), NOT the preferred tree-less #89 `ledgerWrite.applyTransition` CAS. The slice explicitly permits option (b) ONLY IF a follow-up observation is filed for the tree-less surface. No such observation was added (git shows only the two source observations modified, no new one). File the follow-up observation, or ratify dropping it.
  (Slice AC: 'if the surface seam (routeToNeedsAttention, currently cwd-bound) cannot be made tree-less within this slice, surface via the existing path + file a follow-up observation for the tree-less surface.' `ledger-write.ts` applyNeedsAttentionTransition still delegates to the cwd-bound routeToNeedsAttention; work/observations/ has no new tree-less-surface note.)
- The Part-B test for the job-worktree createJob path (the 'eta' test) — which is the EXACT path the original `--isolated` incident hit — only asserts that `createJob` flags `continuePushFailure` and does NOT throw. It never drives `runRemotePipeline`/`runOneItem` to assert the item actually lands in `needs-attention/` on the arbiter and is NO LONGER in `in-progress/`. That core acceptance assertion ('assert it lands in needs-attention/') is covered only for the start.ts path (the 'zeta' test). The do.ts/run.ts routing of `continuePushFailure` → needs-attention is therefore end-to-end untested on the incident's own path. The routing code mirrors the already-tested conflict path, so this is a coverage gap rather than a proven defect — but consider adding an end-to-end assertion for the createJob/run surfacing.
  (stale-lease-all-push-sites.test.ts 'eta' test (Part B describe block) asserts only job.continuePushFailure is defined + work recoverable + no throw. do.ts (runRemotePipeline step 4b) and run.ts (runOneItem step 2b) contain the needs-attention routing; no test exercises that branch for the createJob path. Slice AC: 'assert it lands in needs-attention/.')
- In-scope design decision to ratify: the after-commit surface uses TWO different strategies across the sibling sites. do.ts/run.ts (createJob path) call `applyNeedsAttentionTransition` with cwd = the job worktree (HEAD on the work branch), which commits the wip + ledger-move onto the work branch and re-attempts the work-branch push (default pushBranch). start.ts (`routeContinuePushFailure`) instead cuts a throwaway temp branch off `<arbiter>/main` and uses `pushBranch:false`, restoring afterward. Both are safe (only the move-only commit reaches main via cherry-pick), but the asymmetry is an unrecorded choice — the start.ts variant avoids mutating the human's checkout's work branch and avoids a redundant re-push of the just-failed branch. Ratify the asymmetry (it looks deliberate and correct), or unify if a single seam is preferable.
  (start.ts routeContinuePushFailure cuts agent-runner/continue-push-failure-<slug> off <arbiter>/main, pushBranch:false. do.ts/run.ts pass cwd:tree.dir with no pushBranch override (defaults true → re-pushes work/<slug>, best-effort, captured).)
- In-scope decision to ratify: `isOfflinePushFailure` classifies offline-vs-real using a fixed git-connectivity stderr allowlist (unable to access / could not read from remote / connection refused|reset|timed out / could not resolve host / ssh connect / network unreachable / ECONNREFUSED|ECONNRESET / ENOTFOUND / ETIMEDOUT) and treats the retry-cap give-up as always-real, defaulting any unmatched cause to SURFACE (conservative — never silently swallow). This is the right default direction, but the allowlist is a user-visible classification boundary: a connectivity error phrased outside these patterns would be (safely) surfaced to needs-attention rather than tolerated as offline. Ratify the allowlist + conservative default.
  (start.ts isOfflinePushFailure regex; the helper throws only two shapes (a '(not a stale lease)' raw-stderr failure, or the cap-exhausted give-up message). epsilon test exercises the offline branch with 'Could not read from remote repository'; zeta exercises the surface branch (protected ref).)
