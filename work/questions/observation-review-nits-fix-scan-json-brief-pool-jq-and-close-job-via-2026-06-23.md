<!-- dorfl-sidecar: item=observation:review-nits-fix-scan-json-brief-pool-jq-and-close-job-via-2026-06-23 type=observation slug=review-nits-fix-scan-json-brief-pool-jq-and-close-job-via-2026-06-23 allAnswered=false -->

## Q1

**What should happen to this observation? It is the durable home for four NON-BLOCKING review nits raised when Gate 2 APPROVED the now-done task 'fix-scan-json-brief-pool-jq-and-close-job-via'. Re-checked against current reality, most of the residue has been overtaken by events: (1) the headline 'live workflow still reads .prds[] not .briefs[]' framing is now inverted by the later global brief->prd rename (commit 403a5be) so the live workflow CORRECTLY reads .prds[] and '.briefs[]' no longer exists anywhere in the template or workflow; (2)+(3) the two RATIFY findings (editing docs/ci/advance-loop.yml.template, adding the recursive-test-flake observation) were each judged NECESSARY and by-design, flagged only because no Decisions block recorded them; (4) the stale-prose finding pointed at scan.test.ts / close-job.test.ts strings that no longer exist in any .ts file, so the named prose-sweep tasks have since run. Net: close/delete the note, mint a follow-up task, or keep it as a record?**

> work/notes/observations/review-nits-fix-scan-json-brief-pool-jq-and-close-job-via-2026-06-23.md (needsAnswers: true, reviewOf: fix-scan-json-brief-pool-jq-and-close-job-via, which now lives in work/tasks/done/). All four findings are self-described 'non-blocking', 'by-design, not a defect', 'No action needed in this task'. Verified now: grep for 'briefs[]' returns nothing in docs/ci/advance-loop.yml.template or .github/workflows/advance-lifecycle.yml (both read .prds[]/items[] post-rename); grep for the stale prose strings ('sliceable-PRD pool', 'closes the PRD issue when ALL its') returns no .ts hits. The recursive-test-flake mentioned in finding 3 is its OWN observation item (recursive-test-run-occasional-flake-2026-06-23.md, needsAnswers: true) with its own triage, so it does not need to be handled here. The 'observation has no gate' so this is the native triage question per SURFACE-PROTOCOL §What you COMPOSE item 2.

_Suggested default: Delete the note (direct-delete path). All four findings are stale or discharged: the headline .briefs[]/.prds[] concern is moot after the brief->prd rename, the two RATIFY items were by-design and the work is merged, and the prose-sweep it deferred to has already landed (the flagged strings are gone). The only forward-looking residue was 'schedule the workflow regen so capability B reaches production', and the install-ci-advance-lifecycle-workflow task is already done; if the human wants belt-and-suspenders, mint a one-line follow-up to confirm the live cron picks up the current template rather than keeping this whole nit-bundle open._

<!-- q1 fields: id=q1 -->

**Your answer** (write below this line):
