<!-- dorfl-sidecar: item=observation:review-nits-allow-backlog-leak-fence-assertions-2026-06-24 type=observation slug=review-nits-allow-backlog-leak-fence-assertions-2026-06-24 allAnswered=false -->

## Q1

**Triage this observation: what becomes of the two non-blocking Gate-2 review nits on 'allow-backlog-leak-fence-assertions' (missing '## Decisions' block on a commit that made two in-scope judgement calls — (a) scoping out PRD decision-5's OPTIONAL refuse-if-also-in-ready guard, and (b) shipping assertions-only because the fence was already structurally closed)? Options: (1) promote-task — open a tiny follow-up task to either amend/append a Decisions note in a docs location (the commit itself is already landed and immutable in practice) or to relocate decision (a) to the keystone resolver task as an explicit scope handoff; (2) promote-adr — capture 'leak-fence tasks ship as assertions-only when no leak is found' and/or 'optional guards from PRD decisions land with their keystone task, not their fence task' as durable conventions; (3) keep — leave the observation as the durable record of these nits, no further action; (4) delete — the nits are too minor to keep around once seen; (5) dropped — accept as won't-proceed (record reason in body, e.g. 'commit already landed; conventions implicit'); (6) needs-attention — escalate, the choice is non-obvious.**

> Observation body: Gate 2 APPROVED the task. Nits are two unrecorded judgement calls: (1) PRD decision 5 left an OPTIONAL '--allow-backlog refuse if slug also in ready/' guard to the builder; this fence task neither implemented nor asserted it, deferring to resolveTask's ready-before-backlog precedence — a defensible scope choice but unrecorded. (2) The task prompt said 'do not just assert the happy path' — agent found no leak and shipped assertions only (+332 test lines, 0 src lines, plus the git mv). Both are commit-message/decision-log hygiene findings on an otherwise-approved task. The keystone resolver task 'do-allow-backlog-flag-resolver-claim-and-done-move' still exists in work/tasks/done/ alongside the fence task, so the optional guard's natural home (if anywhere) is THERE, not here.

_Suggested default: promote-adr — finding (2) ('a leak-fence task whose fence is already structurally closed legitimately ships as assertions-only') is a reusable convention worth capturing once so future fence-style tasks don't re-surface the same nit; finding (1) is then naturally absorbed (optional PRD guards belong to their keystone task, not their fence task). If the bar for an ADR feels too high, fall back to 'keep' — the observation itself is already the durable record and no code/doc change is owed._

<!-- q1 fields: id=q1 disposition=promote-adr -->

**Your answer** (write below this line):

keep — the observation itself is the durable record of these two non-blocking nits, and no code/doc change is owed (the commit is landed; both in-scope calls were sound). The "leak-fence ships assertions-only when the fence is already structurally closed" / "optional PRD-decision guards land with their keystone task, not the fence task" conventions are a one-off shape so far (n=1); promoting an ADR now would over-formalise. Re-promote to promote-adr only if the same nit recurs on a future fence-style task.
