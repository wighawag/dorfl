<!-- agent-runner-sidecar: item=observation:release-lock-cannot-name-pre-cutover-slice-prefixed-lock-entries-2026-06-20 type=observation slug=release-lock-cannot-name-pre-cutover-slice-prefixed-lock-entries-2026-06-20 allAnswered=false -->

## Q1

**Triage this observation: does the un-nameable pre-cutover lock entry (release-lock + reaper both key off the current `task-`/`brief-`/`observation-` mapping and so cannot address residual `slice-<slug>` / `prd-<slug>` lock refs left over from the vocabulary cutover) become a slice, an ADR, get kept as a watch-item, dropped, or flagged needs-attention? The note proposes three non-exclusive fix shapes — pick whichever the disposition implies: (a) one-time MIGRATION renaming `refs/agent-runner/lock/slice-<slug>` -> `task-<slug>` and `prd-<slug>` -> `brief-<slug>` on arbiter + mirrors (possibly folded into `gc --ledger`); (b) raw-entry ESCAPE HATCH on `release-lock` (e.g. `release-lock --entry slice-foo`) so a human can name a literal entry regardless of current mapping — preserves the un-guessable-liveness trust model; (c) at minimum, make `gc --ledger`'s REPORT surface the literal entry names and DOCUMENT the raw ref-delete workaround until a migration exists.**

> Observed 2026-06-20: orphan `refs/agent-runner/lock/slice-claim-cas-spinner` could not be cleared by `release-lock task:claim-cas-spinner` (resolves to `task-claim-cas-spinner`, reports already-absent), and had to be removed by raw `git push origin --delete`. Defeats the protocol's trust model where a human clears an orphan by NAMING it via the verb; only recourse is the raw git plumbing the protocol tells operators to avoid. Sibling observation `reaper-never-clears-a-done-plus-stuck-lock-orphans-forever-2026-06-20` explains why the reaper did not clear it either. Affected surfaces: `packages/agent-runner/src/cli.ts` (release-lock + `gc --ledger --reap-stale-locks`), `item-lock.ts` (`lockEntryFor`/`itemLockRef`), `slug-namespace.ts`. Blast radius is bounded to arbiters that held a `slice-`/`prd-` lock across the cutover, but every such lock is currently un-releasable through the verb.

_Suggested default: promote-slice — ship the escape-hatch (b) + report-literal-names (c) together (small, low-risk, immediately unblocks any affected arbiter and preserves the human-asserts-liveness trust model); leave the one-time migration (a) as a follow-up only if more pre-cutover orphans are reported, since it touches ref-rewriting on arbiter + mirrors and the escape hatch already covers the same orphans._

<!-- q1 fields: id=q1 disposition=promote-slice -->

**Your answer** (write below this line):
