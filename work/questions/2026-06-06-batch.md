# BATCH 2026-06-06 — studying: runner-in-ci, issue-to-prd, issue-intake, review   (the PRDs in THIS batch; ephemeral)

> Scope = `work/prd/` only (this run). GATHER pass only — answer the `> ANSWER:`
> lines in one sitting, then a follow-up run will APPLY them. This file writes
> nothing back to the PRDs and commits nothing.
>
> **Studied set (the un-resolved PRDs reviewed this run):** `runner-in-ci`,
> `issue-to-prd`, `issue-intake`, `review`.
>
> **Skipped as already cleanly sliceable / resolved (NOT in this batch):**
> `agent-runner` (foundational PRD — its core slices `scan`/`run-once`/`start`/
> `claim-command`/`verify`/`complete` are already in `work/done/`; no `sliceAfter`,
> no open questions), and the six PRDs already carrying a `sliced:` marker:
> `auto-slice`, `batch-qa`, `command-surface-phase-2`, `ledger-transition-seam`,
> `needs-attention-cherry-pick`, `review-skill`.
>
> **No PRD has `needsAnswers: true` in frontmatter and none carries a literal
> `## Open questions` block.** The pre-existing questions below come from `review`'s
> `needsAnswers` bullet-list (in its Autonomy notes); everything else is
> review-skill-surfaced (slice-readiness).

---

## PRDS (slice-readiness)

### runner-in-ci  [prd · needsAnswers: no · NOT yet sliced]

context: No `sliceAfter` dependency — this PRD is the *root* of the issue-front-door
chain (`issue-to-prd` and `issue-intake` both `sliceAfter` it). So slicing this one
unblocks the other three. Its launch readiness says `needsAnswers: none` (auth
modes + trigger decided, mirror whitesmith). Review surfaced the questions below.

Q1 (review-surfaced · claim-vs-reality): The PRD is written around the `do` command
(`agent-runner do`, `do --propose`, `do -n`, `do <prd>`, in-place CI isolation). It
was RESHAPED 2026-06-05 from `run --once` → `do`. Are all of `do`'s referenced
shapes (`do` auto-pick, `do -n <x>`, `do <slug>`/`do <prd>`, `--propose`, in-place
isolation) **landed and stable** today, so a slice can build `install-ci` on top
without the engine surface still moving under it?   [suggested default: YES — `do-in-place`, `do-autopick`, `do-watch`, `isolation-strategy-seam` are all in `work/done/`; treat the `do` surface as stable ground for slicing `install-ci`. If any `do` shape is still in flight, name it and I'll add a `blockedBy`.]
> ANSWER: assess yourself and report

Q2 (review-surfaced · destination check): The PRD says `install-ci` is
**per-capability** (flag-selected) and mentions auto-slice in the CI tick (`do`
slices ready PRDs too). Is the auto-slice-in-CI behaviour **in scope for the
`runner-in-ci` slices**, or strictly deferred to `auto-slice`'s own slices (so
runner-in-ci only wires the workflow + auth + `do` invocation, and any
PRD-slicing-in-CI is owned elsewhere)?   [suggested default: DEFER — runner-in-ci slices own only the workflow scaffold + auth + the `do` invocation; "`do` happens to also slice PRDs" is `auto-slice`/`do` behaviour already built, not a runner-in-ci deliverable. Keeps the slice boundary clean.]
> ANSWER: assess yourself and report

Q3 (review-surfaced · gate-axis honesty): `humanOnly: true` is DECIDED at PRD level
(workflows + secrets are security-sensitive). The PRD then says "most covering
slices should ALSO be `humanOnly`, especially anything that writes workflows or
touches secrets." For slicing: should the slicer mark **every** runner-in-ci slice
`humanOnly`, or only the workflow-writing / secret-touching ones (leaving e.g. a
pure `--fake`-output generator test slice agent-buildable)?   [suggested default: PER-SLICE — only slices that emit workflow YAML or set/read secrets are `humanOnly`; a pure deterministic generator-with-`--fake`-snapshot slice can be agent-buildable. Decide per slice from building it, not inherited wholesale from the PRD.]
> ANSWER: PRD humanOnly should not drive slice humanOnly, only the to-slices process will decide

### issue-to-prd  [prd · needsAnswers: no · NOT yet sliced · BLOCKED]

context: `sliceAfter: [auto-slice, runner-in-ci]`. `auto-slice` IS sliced (✓), but
**`runner-in-ci` is NOT yet sliced** (no `sliced:` marker) — so by its own
`sliceAfter` contract this PRD is **not slice-ready yet**: its slices need
runner-in-ci's slugs to exist to reference in `blockedBy`. Its own content is
`needsAnswers: none` and reads as decided (seam, trigger spectrum, unified
conversation rule, option-(iii) closure).

Q1 (review-surfaced · BLOCKING dependency): Confirm the intended order: **slice
`runner-in-ci` FIRST** (a prior batch-qa run / a `to-slices` pass), THEN slice
`issue-to-prd` so its `blockedBy` can reference the real `install-ci`/auth slugs?
Or do you want to relax `sliceAfter` and slice `issue-to-prd` now against
*planned* slugs?   [suggested default: ORDER IT — do NOT slice issue-to-prd this cycle; slice runner-in-ci first, then issue-to-prd in a later run. (batch-qa advances one rung per run anyway; this just respects the declared `sliceAfter`.)]
> ANSWER: ok yes order it

Q2 (review-surfaced · claim-vs-reality, only relevant once unblocked): The closure
glue depends on the core read-only **"is this PRD complete?"** query (all `prd:<slug>`
slices in `work/done/`, ≥1). Does that query **already exist** in the codebase
(some `gc`/scan/complete path computes it), or is building it part of THIS PRD's
slices?   [suggested default: PART OF THIS PRD — assume the "PRD complete?" predicate is a deliverable of an issue-to-prd slice unless a `done/` slice already exposes it; the slicer should add it as its own slice (pure `work/`-folder logic, agent-buildable) that the close-glue consumes.]
> ANSWER: part of this prd unless already done, can check when working on it

### issue-intake  [prd · needsAnswers: no · NOT yet sliced · BLOCKED]

context: `sliceAfter: [auto-slice, runner-in-ci, issue-to-prd]`. `auto-slice` ✓ but
**BOTH `runner-in-ci` AND `issue-to-prd` are NOT yet sliced** → two unmet
`sliceAfter` deps; this is the most-downstream PRD in the chain. Content is decided
(slices-first, count-based PRD trigger, three outcomes, the shared author-trust
resolver).

Q1 (review-surfaced · BLOCKING dependency): Same ordering question, one level
deeper — issue-intake should be sliced **LAST** of the chain (after both
runner-in-ci and issue-to-prd are sliced). Confirm we do NOT touch issue-intake
this cycle?   [suggested default: ORDER IT — issue-intake is sliced last; not this cycle. It needs issue-to-prd's slugs (it reuses that seam/conversation) AND runner-in-ci's `install-ci` slugs.]
> ANSWER: Order it

Q2 (review-surfaced · cross-artifact composition — the SHARED PRIMITIVE): issue-intake's
**author-trust resolver** (author association × request channel × repo policy) is
declared to be the **SAME primitive** `review`'s PR `autoMerge`-on-approve uses
("define it ONCE, consumed by both"). `review` lists this exact thing as one of its
OPEN questions (see review Q4 below). So this shared primitive is currently
**unowned across two un-sliced PRDs**. Who OWNS building it — issue-intake,
`review`, or a third foundational slice both `blockedBy`?   [suggested default: ONE OWNER + blockedBy — define the author-trust resolver as a single foundational slice (owned by whichever of the two is sliced first, likely surfacing under runner-in-ci/issue-to-prd substrate), and have the other PRD's slices `blockedBy` it. Do NOT let both PRDs each grow their own copy. Pin the owner when answering review Q4 so the two answers agree.]
> ANSWER: leave it make it needsAnswer, the authorship is about the issue but we want it to be possible for issue-intake to be processed given a cmmand (/command in an issue comment) by repo owner, etc...

### review  [prd · needsAnswers: no (frontmatter) BUT 4 explicit open questions in body · NOT yet sliced · BLOCKED]

context: `sliceAfter: [auto-slice, runner-in-ci, review-skill]`. `auto-slice` ✓ and
`review-skill` ✓ (sliced 2026-06-06), but **`runner-in-ci` is NOT yet sliced** →
one unmet `sliceAfter` dep. MORE IMPORTANTLY, this PRD's **Autonomy notes carry a
real `needsAnswers` block with FOUR open questions** (pre-existing — copied inline
below). Even though the frontmatter doesn't set `needsAnswers: true`, these are
genuine blocking slice-readiness questions: a slicer cannot crisply cut these slices
without them. This is the one PRD in the batch that is **substantively** not
slice-ready (the other three are mostly ordering-blocked).

Q1 (pre-existing · "Context isolation"): How separate must the reviewer's context
be from the producer's to avoid rubber-stamping — fresh subagent? different model?
just a hard adversarial reframe? What is the **cheapest thing that actually
de-correlates**?   [suggested default: FRESH-CONTEXT subagent as the floor (a cold read — the `review` skill already insists on this), with the per-repo `review` MODEL OVERRIDE (already in §13) as the opt-in stronger de-correlation. Don't mandate a different model by default; mandate fresh context + adversarial reframe, allow model-override per repo.]
> ANSWER: fresh context is enough for now, but yet model should be configurable specificly for reviews

Q2 (pre-existing · "Role vs grilling"): Is "grilling" a DISTINCT role from "review",
or the same step with an adversarial prompt? (ADR `execution-substrate-decisions.md`
§13 staged both.)   [suggested default: SAME STEP, adversarial prompt — one `review` role/skill; "grilling" is review run with the adversarial-reframe dialed up, NOT a second role. Avoids a second seam. (The `review` skill already bakes "be adversarial" into its disciplines.)]
> ANSWER: same step

Q3 (pre-existing · "Iteration bound"): Exact `reviewMaxRounds` shape + where
exhaustion routes (the revise↔review loop must not run forever — the Testing
Decisions already assume a `reviewMaxRounds` test exists).   [suggested default: `reviewMaxRounds` per-repo (resolved flag > per-repo > global > built-in default, default e.g. 2–3), and on exhaustion FORCE needs-attention (route the item to `needs-attention/`, never silently merge or loop). This matches the Testing Decisions bullet "on exhaustion it forces needs-attention."]
> ANSWER: yes your suggestion work, and we can error out on max reached

Q4 (pre-existing · "Auto-merge trust model" — THE SHARED PRIMITIVE, ties to issue-intake Q2):
Does `autoMerge` key ONLY on repo policy, or also on the work's AUTHOR / how it was
requested (the same `issue-intake` author-trust policy)? Decide whether the two
share ONE trust primitive, and — critically — **who builds it**.   [suggested default: ONE SHARED PRIMITIVE, built once. `autoMerge` keys on repo policy AND author-trust (author association × channel × repo policy), the SAME resolver issue-intake uses. Name a single owning slice both PRDs `blockedBy`. Make this answer AGREE with issue-intake Q2 (same owner).]
> ANSWER:  need to think more about the user stories here to give an answer, I initialy though an autoMerge config is enough but you raise a good point

Q5 (review-surfaced · BLOCKING dependency / ordering): Independent of the 4
questions above — `review` still has `runner-in-ci` unmet in `sliceAfter`. Slice
order: runner-in-ci → (review-skill ✓ already) → review. Confirm `review` is sliced
only AFTER runner-in-ci is sliced AND Q1–Q4 are answered?   [suggested default: YES — review is NOT sliceable until (a) runner-in-ci is sliced and (b) Q1–Q4 are resolved. It is the most genuinely-blocked PRD; expect it to need its own later run even after this batch.]
> ANSWER: yes

---

## READINESS (2026-06-06, round 1 — APPLIED)

Answers merged into the PRD bodies; each PRD advanced exactly ONE rung (answers
applied — NOT sliced this run, per the one-step invariant and your ordering).

  ANSWER-RESOLVED (slice-readiness questions settled; now order-blocked only, await `runner-in-ci` slicing):
    - runner-in-ci  — Q1 ASSESSED (do <slug> stable; auto-pick/`-n`/`do prd:`/`--remote` NOT yet wired → slices must `blockedBy` `do-autopick` etc.), Q2 deferred auto-slice-in-CI, Q3 PRD-humanOnly does NOT drive slice humanOnly (to-slices decides). No frontmatter `needsAnswers` needed.
    - issue-to-prd  — Q1 order-it (runner-in-ci first), Q2 ASSESSED (no PRD-complete query exists today → it's a deliverable slice). No open questions remain → stays `needsAnswers`-clean; purely order-blocked.

  STILL OPEN (now `needsAnswers: true` in frontmatter — ONE real question each, SHARED):
    - issue-intake  — author-trust resolver shape + OWNER (you steered: trust is about the issue author, but a repo owner/maintainer `/command` must be able to authorize). Also sliced LAST of the chain.
    - review        — Q1/Q2/Q3 RESOLVED (fresh-context + per-repo review-model override; same-step grilling; `reviewMaxRounds` → error-out to needs-attention). Q4 STILL OPEN: auto-merge trust model — you want to think on the user stories. SAME primitive as issue-intake.

  THE SHARED OPEN PRIMITIVE: issue-intake's resolver ⇔ review Q4 are the SAME
  author-trust primitive — resolve TOGETHER, ONE owning slice both PRDs `blockedBy`.
  Both now carry `needsAnswers: true` so neither is sliced until it's pinned.

  NEXT (a later batch-qa run, NOT this one):
    1. slice `runner-in-ci` (now slice-ready) → then issue-to-prd → then issue-intake.
    2. resolve the shared author-trust primitive (issue-intake + review Q4) → clears both `needsAnswers`.
    3. then slice `review` (after runner-in-ci sliced AND the primitive settled).

  This file is now ephemeral/redundant (answers live in the PRDs) — safe to delete; git history is the archive. NOT committed/moved by this pass.
