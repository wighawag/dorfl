<!-- dorfl-sidecar: item=observation:decisions-block-convention-repeatedly-skipped-enforce-or-relax-2026-06-22 type=observation slug=decisions-block-convention-repeatedly-skipped-enforce-or-relax-2026-06-22 allAnswered=false -->

## Q1

**Is the `## Decisions` block convention load-bearing enough to ENFORCE (a checked gate in the acceptance / lint step that fails a slice missing a `## Decisions` block when an in-scope non-obvious decision was made), or should it be RELAXED to "a durable record ANYWHERE checkable" (JSDoc at the choice site, a Decisions block, or an observation — with a link), or a HYBRID (Decisions block REQUIRED for slice-level judgement calls, JSDoc explicitly blessed for narrow local code invariants)? The answer routes this observation: pick a direction and it becomes a protocol-doc-edit slice (`promote-slice`) that updates `work/protocol/CLAIM-PROTOCOL.md` and `work/protocol/task-template.md` (mirrored byte-identically into `skills/setup/protocol/` per AGENTS.md); decide the convention is fine as-is and the five waivers were one-offs and it is `keep`; decide the signal is real but not worth acting on right now and it is `dropped` (reason: won't-fix / accept-the-drift).**

> Observation `work/notes/observations/decisions-block-convention-repeatedly-skipped-enforce-or-relax-2026-06-22.md` records that across a single 2026-06-22 `answer-questions` triage, FIVE separate review-nit sidecars hit the same shape: a slice/prompt mandated a `## Decisions` block, the decision WAS made and IS durably recorded (module JSDoc next to the code, or the observation itself), but NOT in a literal `## Decisions` block, and the maintainer ratified all five as keep/dropped ("the existing record is the durable home") rather than reopening the slices. The five: `review-nits-question-sidecar-human-readable-format-2026-06-20` q1 (sidecar.ts JSDoc), `review-nits-reaper-no-lock-outcome-benign-not-lost-2026-06-20` q1 (JSDoc on `reapReportNeedsAttention`; AC#4 literally asked for a Decisions block), `review-nits-remove-dead-needs-attention-folder-readers-after-lock-cutover-2026-06-22` q1 (never recorded as a Decisions line), `review-nits-review-protocol-doc-and-shared-machinery-2026-06-22` q4 (three in-scope decisions recorded only in the observation), `review-nits-work-layout-guard-catch-absolute-prefix-path-literals-2026-06-20` q1 (rationale in an in-source test comment). The convention currently lives in `work/protocol/CLAIM-PROTOCOL.md` ("end your report with a `## Decisions` block, one entry per decision") and `work/protocol/task-template.md` ("RECORD non-obvious in-scope decisions"); WORK-CONTRACT.md does NOT mention it. The observation explicitly does NOT pick a direction — it surfaces the choice. Trade-offs are spelled out in its "Two honest directions" section: ENFORCE gains reliability at the cost of new gate machinery and likely hollow-block gaming; RELAX matches actual behaviour with zero machinery but loses the single-place-to-look property; HYBRID needs the slice-decision-vs-local-invariant line drawn crisply.

_Suggested default: promote-slice with the HYBRID direction: keep `## Decisions` REQUIRED for slice-level judgement calls (the kind a Gate-2 reviewer would flag and the kind that benefits from one discoverable home), explicitly bless JSDoc-at-the-choice-site for narrow local code/parser/predicate invariants (which is where four of the five waived instances actually landed), and update CLAIM-PROTOCOL.md + task-template.md (mirrored to `skills/setup/protocol/`) to state the line. Rationale: the 5/5 ratification pattern is real evidence the strict rule is wrong, but pure RELAX ("anywhere") loses the discoverability that made the rule attractive; HYBRID captures both. Offered as a humility aid only — the maintainer owns the call._

<!-- q1 fields: id=q1 -->

**Your answer** (write below this line):
