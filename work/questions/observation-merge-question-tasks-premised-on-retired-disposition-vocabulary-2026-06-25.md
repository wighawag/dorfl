<!-- dorfl-sidecar: item=observation:merge-question-tasks-premised-on-retired-disposition-vocabulary-2026-06-25 type=observation slug=merge-question-tasks-premised-on-retired-disposition-vocabulary-2026-06-25 allAnswered=false -->

## Q1

**What becomes of this observation — specifically, do you want to re-open the PRD `land-time-reverify-and-parallel-merge-ceiling` (currently in `prds/tasked/`) and re-decompose its merge-question-related tasks against the new agentic / binary-sidecar model before any of them are built, or take some other path (e.g. amend the affected tasks in place, drop them, or defer)?**

> The observation reports that retiring the sidecar disposition vocabulary (merged PRD `agentic-question-resolution-retire-disposition-vocabulary`) has invalidated the premise of three sibling tasks under `land-time-reverify-and-parallel-merge-ceiling` (in `prds/tasked/`):
>   - `tasks/backlog/merge-question-surfacer.md` — its `## What to build`, a scope bullet, and an acceptance criterion all require emitting `merge | hold | drop` disposition tokens into the sidecar; there is no longer a `disposition=` field (entries are binary no-answer | answered), so as written the task cannot be built.
>   - `tasks/backlog/apply-rung-merge-disposition.md` — specifies extending `triage-persist.ts`'s `promote-slice`/`dropped` disposition-dispatch to handle an answered `merge` disposition; that dispatch (and `pickTerminal`, `answeredPromoteArtifact`) was removed by the keystone, `merge` is not a `DecisionOutcome`, so there is no token-dispatch left to mirror.
>   - `tasks/backlog/merge-questions-gate-axis.md` — only gates whether the surfacer runs, so it is less directly drifted but depends on the surfacer's reshape.
> The PRD's GOAL (surface unmerged `work/*` branches → human answers → apply LAND: rebase → re-verify → advance) is still valid; only the MECHANISM (disposition tokens) is gone. The observation's suggested next step is to re-task the PRD against the new model before any merge-question task is promoted to the build pool, and it sketches a wired-in re-scope: treat answer-driven runner ACTIONS (merge/land, sibling stuck-lock requeue) as a DISTINCT dispatch layer keyed off the surfaced question's identity + the human's plain answer, rather than forcing `merge` into the `decide()` content-outcome union — to be resolved consistently with that PRD's two existing open cross-cutting questions (sidecar→branch/lock-ref keying; questions-folder shape).

_Suggested default: Re-open `prds/tasked/land-time-reverify-and-parallel-merge-ceiling.md` and re-decompose its merge-question tasks against the new agentic / binary-sidecar model along the sketched re-scope (answer-driven runner ACTIONS as a distinct dispatch layer, NOT a `decide()` outcome), resolving its two existing open cross-cutting questions in the same pass; until that re-decompose lands, hold `merge-question-surfacer`, `apply-rung-merge-disposition`, and `merge-questions-gate-axis` out of the build pool. Then delete this observation as discharged._

<!-- q1 fields: id=q1 -->

**Your answer** (write below this line):

Re-open `prds/tasked/land-time-reverify-and-parallel-merge-ceiling.md` and re-decompose its merge-question tasks against the new agentic / binary-sidecar model. The PRD's GOAL stays valid; only the disposition-token MECHANISM is gone. Use the sketched re-scope: treat answer-driven runner ACTIONS (merge/land, sibling stuck-lock requeue) as a DISTINCT dispatch layer keyed off the surfaced question's identity plus the human's plain answer, NOT forced into `decide()`'s content-outcome union. Resolve the PRD's two existing open cross-cutting questions (sidecar->branch/lock-ref keying; questions-folder shape) in the same pass. Until the re-decompose lands, HOLD `merge-question-surfacer`, `apply-rung-merge-disposition`, and `merge-questions-gate-axis` out of the build pool. Then delete this observation as discharged.
