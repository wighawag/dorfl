<!-- dorfl-sidecar: item=observation:triage-observations-skill-ignores-pending-questions-2026-06-20 type=observation slug=triage-observations-skill-ignores-pending-questions-2026-06-20 allAnswered=false -->

## Q1

**What should become of this signal? The observation reports that the triage-observations skill's INVESTIGATE step omits the pending-questions surface (open sidecars in work/questions/ and an item's needsAnswers/## Open questions state) from its current-reality sources, so triage can discharge a note whose matter is genuinely still open. Options the human can ratify: (a) amend the skill so step 2's investigate-sources list adds the pending-questions surface and the recommendation guidance gains a "leave — blocked on <question>" note; (b) promote it into a self-contained skill-amendment task; (c) leave it on record as a known process gap; or (d) delete it. Which?**

> VERIFIED STILL LIVE against current reality. The skill at ~/.agents/skills/triage-observations/SKILL.md step 2 (line 18) enumerates investigate-sources as "the actual code, tasks, prds, ADRs, and protocol docs it references" — it does NOT include the question/answer sidecars (work/questions/<type>-<slug>.md, src/sidecar.ts) nor an item's declared-open needsAnswers/## Open questions state. So the gap the note describes is unaddressed. NOTE one part of the note is now STALE: it frames the impact in terms of the old disposition token vocabulary (leave/delete/make-task/amend/fold-into-ADR), but that token round-trip has since been RETIRED (see done task work/tasks/done/triage-observations-skill-retire-disposition-vocabulary.md; the skill now recommends in plain terms). The underlying investigate-blind-spot is independent of that vocabulary change and remains real. Scope is a SKILL-doc change, not repo code. This is a process/skill-quality signal. The note has NO existing question sidecar of its own, and the sibling related note work/notes/observations/question-sidecar-has-no-visible-link-to-the-item-it-asks-about-2026-06-20.md is still spotted/needsAnswers (same answer-loop surface, different gap).

_Suggested default: Amend the skill (option a): add the pending-questions surface (work/questions/ sidecars + needsAnswers/## Open questions) to step 2's investigate-sources list, with exact-item match treated as cheap/required and topical overlap surfaced to the human rather than auto-decided (consistent with the skill's never-auto-decide rule); add a "leave — blocked on <question>" reasoning note to the recommendation guidance. This is a low-risk doc change directly addressing a verified live blind spot._

<!-- q1 fields: id=q1 -->

**Your answer** (write below this line):

Amend the skill (option a). Add the pending-questions surface (work/questions/ sidecars plus an item's needsAnswers / ## Open questions state) to step 2's investigate-sources list, with an exact-item match treated as cheap and required, and topical overlap surfaced to the human rather than auto-decided (consistent with the skill's never-auto-decide rule). Add a "leave, blocked on <question>" reasoning note to the recommendation guidance. Low-risk doc change against a verified live blind spot. Mint it as a small skill-amendment task (this can be the same E task from the sidecar-rebuild-sweep note). Note the stale disposition-token framing in the observation body is superseded by the retired-vocabulary cutover; the investigate blind spot is independent and still real.
