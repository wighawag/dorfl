<!-- dorfl-sidecar: item=observation:clean-break-other-rename-narration-phrases-survive-2026-06-23 type=observation slug=clean-break-other-rename-narration-phrases-survive-2026-06-23 allAnswered=false -->

## Q1

**What becomes of this signal? Five rename-narration phrases survive the clean-break sweep that match task `clean-break-context-adr-and-skills-prose` AC #5/#43's generic patterns ("renamed from"/"used to be"/"the old name") but each narrates a DIFFERENT, legitimate rename (pr->propose, return->requeue, untrusted-origin prior-state, allowAgents->removed) rather than slice->task. Do you want a follow-up prose task to ALSO strip these other-rename clauses, or is the slice->task-scoped sweep the intended final state (keep these clauses, close the observation)?**

> Verified against current reality (2026-06-25): all 5 hits still present and each narrates a non-slice->task rename — execution-substrate-decisions.md:75 "(Renamed from `pr`...)"; command-surface-and-journeys.md:120 "requeue (renamed from return)"; untrusted-origin-build-checkpoint.md:17 "used to be invisible"; skills/setup/SKILL.md:254 + WORK-CONTRACT.md:198 "renamed from the now-removed allowAgents". The slice->task sweep residue itself is clean (zero `code-identifier-` notes, zero `as-yet-unrenamed`/`NOT yet reflected`). The tension is real: the maintainer's VERBATIM rule + the task goal both scoped the mandate to slice/prd/slicing only, but AC #5/#43 stated the generic phrase-patterns with no such scoping. So this is an editorial decision about the broader AC wording's intent, not a slice/prd-rename miss. The observation was filed precisely to make this choice explicit, not silently dropped.

_Suggested default: Keep the clauses and close the observation: each surviving phrase truthfully narrates a real, distinct rename and reads as legitimate prose, not slice->task transition scaffolding. The maintainer's verbatim rule scoped the clean break to slice/prd/slicing, so these are out of mandate. Stripping accurate rename-narration about OTHER renames is a separate editorial preference that should be its own explicitly-authored task if wanted, not inferred from this sweep's generic AC wording._

<!-- q1 fields: id=q1 -->

**Your answer** (write below this line):
