<!-- dorfl-sidecar: item=observation:pi-yields-turn-early-with-work-pending type=observation slug=pi-yields-turn-early-with-work-pending allAnswered=false -->

## Q1

**What should become of this signal: the observation that pi (the harness) occasionally ends its turn and hands control back to the user, waiting for input, while the agent still has queued work, recoverable only by a human typing `continue`? Keep it as a deferred/recorded observation to correlate against if it recurs, mint a backlog task or PRD, escalate it upstream to the pi maintainers as a repro lead, or drop it?**

> work/notes/observations/pi-yields-turn-early-with-work-pending.md, status: open, needsAnswers: true. The note is explicit that this is an EXTERNAL pi harness-level behaviour, NOT a dorfl protocol/work-contract issue: the runner does not own the conversation turn and cannot detect or recover from an early yield, so there is no dorfl gate for `review` to assess and the triage question is the native 'what becomes of this signal?'. The claim is a spotted live signal (maintainer wighawag, 2026-06-14), self-described as intermittent ('quite rare but it happens') and NOT reproducible on demand, so it is unverified ground truth about a pi code path rather than a confirmed defect in this repo. Verified against current work/: no task or PRD addresses this signal (grep of tasks/ and prds/ for yield/continue/watchdog returns only unrelated rebase-`--continue` recovery items), and no question sidecar exists yet. The note distinguishes itself from pi-harness-jsonl-reliance.md (a separate .jsonl-coupling concern, which WAS triaged toward a deferred backlog 'pi-harness-polish' task) and from the drive-backlog 'interrupt footgun' (a killed wrapper leaving a live child editing, roughly the inverse). Its own 'Candidate follow-ups (not actioned)' section already names the two plausible actions and flags the hard part: capturing the session id/.jsonl + pi version around an early yield to give pi maintainers a concrete repro lead, and noting that any auto-`continue` watchdog would belong in pi (it owns the turn), not the runner, and would have to distinguish a genuine 'agent is done' turn-end from a premature one.

_Suggested default: Keep it as a deferred, recorded observation (do not mint a dorfl task or PRD): the root cause and any fix live in pi, not in the dorfl protocol, and the behaviour is rare, intermittent, and not reproducible on demand, so there is nothing actionable for this repo to build now. The honest next step the note itself scopes is to capture the session id/.jsonl and pi version the next time it recurs and route that upstream to the pi maintainers as a repro lead, rather than acting on it here or discarding the pattern._

<!-- q1 fields: id=q1 -->

**Your answer** (write below this line):
