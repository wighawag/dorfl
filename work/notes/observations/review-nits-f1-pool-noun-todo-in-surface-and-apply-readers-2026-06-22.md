---
title: review-gate non-blocking nits for 'f1-pool-noun-todo-in-surface-and-apply-readers' (Gate 2 approve)
date: 2026-06-22
status: open
reviewOf: f1-pool-noun-todo-in-surface-and-apply-readers
---

## Non-blocking review findings

The PR/code review gate (Gate 2) APPROVED 'f1-pool-noun-todo-in-surface-and-apply-readers' but raised the
following non-blocking findings (nits). They do not block integration; this
is their durable home for triage — promote-to-slice / keep / delete.

- No `## Decisions` block was added to the slice file or commit body, but several non-obvious in-scope decisions WERE made and should be recorded for human ratification. Please ratify (or revert) each: (a) a NEW helper `warnDeprecatedConfigValues` was introduced in `config.ts` (parallel to the existing `warnDeprecatedConfigKeys`) as the value-rename shim pattern — this is a NEW codified pattern, not a reuse of the existing key-removal pattern, even though the slice prompt asked to use 'whatever migration shim the codebase's existing config-rename pattern uses'; (b) the legacy `'backlog'` migration shim was wired at THREE input seams (env in `env-config.ts`, config files via `warnDeprecatedConfigValues` in `config.ts`+`repo-config.ts`, and the CLI flag in `cli.ts`), each with its own one-line warning text; (c) `landingToSide` in `slicing.ts` ALSO defensively accepts the literal `'backlog'` value belt-and-suspenders, even though all three input seams already migrate it — this widens the runtime accept-set beyond the declared `'pre-backlog' | 'todo'` type; (d) `BacklogItem` and `readBacklogItems` are kept as deprecated TYPE/FUNCTION ALIASES re-exported from `scan.ts` / `index.ts` for the migration window (the slice says 'rename to `todo` consistently' but tolerates aliasing — this is a deliberate non-breaking-export call); (e) `prdsLandIn` was inspected and intentionally NOT renamed because its value space is `'pre-prd' | 'prd'` and never carried the `backlog`-means-pool noun — that is the right reading of the slice's 'if either still accepts `backlog`' clause, but it should be recorded so a later reader does not think it was missed.
  (Slice prompt: 'RECORD non-obvious in-scope decisions (e.g. how an existing config-rename shim was reused, whether a `'backlog'` enum value was kept as a deprecated alias) per the task template's RECORD rule.' Commit message is one line; slice body in `work/tasks/done/` is unchanged from the brief — no decisions section was appended.)
- `skills/setup/protocol/WORK-CONTRACT.md` (and its mirror `work/protocol/WORK-CONTRACT.md`) line 221 still documents the PR-free `--merge + land-in-staging` operator flow as `slicesLandIn: backlog` (or `--slices-land-in backlog`). Post-F1 the value `'backlog'` is the DEPRECATED alias of `'todo'` (the POOL) — an operator who follows this protocol doc will (a) get a deprecation warning and (b) land slices in the POOL, the OPPOSITE of what the doc text describes ('land DURABLY on `main` under `work/tasks/backlog/` (the staging folder, NOT eligible)'). The slice's acceptance criterion 'Mirror the same vocabulary fix into work/protocol/ if any contract doc carries the old `backlog`-means-pool noun' is debatable here — the doc uses `slicesLandIn: backlog` to mean STAGING, not pool, so it does not literally carry the old `backlog`-means-pool noun — but the doc is now actively misleading because of the F1 rename. Should the next F-slice tighten line 221 to `slicesLandIn: pre-backlog` (the current correct staging value), or is this deferred to the rest of STEP-B that flips `pre-backlog → backlog` for staging? Same applies in spirit to line 226 ('`slicesLandIn` names the SLICE-side pool/staging slots, now `todo`/`backlog`.') which now reads ahead of the actual `slicesLandIn` enum (`'pre-backlog' | 'todo'`).
  (`grep -n 'slicesLandIn' skills/setup/protocol/WORK-CONTRACT.md work/protocol/WORK-CONTRACT.md` shows lines 221 + 226 untouched by this commit. F1 made zero protocol-doc edits (see `git show --stat b9baf41`).)
- `placement-precedence.test.ts` (lines 184/193/228/239/248/257/275/320/370 etc.) and `pre-backlog-staging-and-promote.test.ts` (lines 423/427/458/461/472) still pass `slicesLandIn: 'backlog'` literally to `performSlice` / `writeRepoConfig`. They compile because `tsconfig.json` includes only `src/**/*.ts`, so test files are not strictness-checked against `PerformSliceOptions.slicesLandIn: 'pre-backlog' | 'todo'`. At runtime they pass because `landingToSide` defensively accepts `'backlog'`. Net effect: a slab of existing tests now silently exercises the DEPRECATED alias path rather than the new vocabulary, and their `describe` titles ('slicesLandIn: backlog + trusted origin ⇒ lands in `work/tasks/todo/`') read incoherently against the rename. Worth a follow-up sweep to retarget them onto `'todo'` (the new spelling) and add ONE dedicated test that the legacy `'backlog'` value emits the deprecation warning and still maps to pool.
  (Grep above; `landingToSide` signature in `slicing.ts` widens to `'pre-backlog' | 'todo' | 'backlog' | undefined` specifically to keep these tests green.)
- Coherence check on the new helper `warnDeprecatedConfigValues`: its sibling `warnDeprecatedConfigKeys` mutates the parsed object by DELETING the offending key, but `warnDeprecatedConfigValues` mutates by REPLACING the offending value in place. Both shapes are reasonable but they diverge from each other — and from the env-side migration which is inlined per-key inside `envOverrides`. Is the intent that future value-renames will all register through `warnDeprecatedConfigValues` (which would benefit from a table-driven shape like `DEPRECATED_CONFIG_KEYS` so a second entry does not require touching the function body), or is this a one-off shim sized to today's single rename?
  (`packages/agent-runner/src/config.ts` added `warnDeprecatedConfigValues` as a hand-rolled `if (parsed.slicesLandIn === 'backlog') …` block; `env-config.ts` open-codes the same migration inline at the `envOverrides` coercion site rather than calling the shared helper.)
