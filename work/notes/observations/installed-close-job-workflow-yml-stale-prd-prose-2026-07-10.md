# Installed `.github/workflows/close-job.yml` still carries old `prd`-prose comments

Date: 2026-07-10

While flipping the dead `prd:` field/verb prose in `packages/dorfl/src` for `hard-cutover-remove-last-prd-back-compat-key-and-dead-verb`, I updated `src/close-job-template.ts` (the generator) to say `spec:` / `spec complete?`. The MATERIALIZED copy at `.github/workflows/close-job.yml` (dorfl's own installed close-job workflow) still has the OLD comment prose (`task.prd:`, `prd:<slug>`, `prd complete?`). It is a generated artifact regenerated by `dorfl install-ci`, is NOT walked by the leak scans (they cover `packages/dorfl/src` + `docs`/`skills`/`work`, not `.github/`), and no test compares it byte-for-byte to the template, so nothing fails. Out of scope for this task (its surface is `src` + the coupled tests + the scans). Left as-is; a `dorfl install-ci` re-run (or the next workflow-template touch) will refresh it. Flagged so the drift is visible.
