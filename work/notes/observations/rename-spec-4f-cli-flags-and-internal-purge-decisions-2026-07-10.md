---
title: prd→spec batch 4f decisions — clean-break CLI flags landed, scan --json `prds[]` output field renamed to `specs[]` (workflow-regen follow-up), and the survivors deliberately LEFT (verdict-content keys, the `<prd>` protocol-template cluster, the `--prds-land-in` input plumbing)
date: 2026-07-10
---

## Context

Task `rename-spec-intake-cli-flags-and-residual-prd-identifiers` (prd→spec MIGRATE batch 4f). Drift-check passed: 4e is in `work/tasks/done/` (residual exported symbols + `PrdsLandIn` plumbing landed; `STAGED_PRDS_DIR` + the `--merge-prd`/`--propose-prd` flag family were still `prd`-spelled + un-twinned at the start, as the task premised). Full gate green: `pnpm -r build && pnpm -r test && pnpm format:check` (2921 dorfl tests pass).

Recording the choices a reviewer/human might be surprised were decided here (per the CLAIM-PROTOCOL decision-recording rule), linked from the done record.

## Decisions

### D1 — `scan --json` output field `prds[]` → `specs[]` (a MACHINE OUTPUT CONTRACT change)

The task's local-var list says rename `prds`→`specs` "file-local, rename freely" and names `scan.ts`/`mirror-pool-scan.ts`. But `RepoReport.prds` (scan.ts:172) is the **`scan --json` OUTPUT field**, `JSON.stringify`'d and read by the CI propose-matrix `jq` as `.repos[].prds[]?` / `.cwd.repo.prds[]?` (advance-lifecycle-template.ts + docs/ci/advance-loop.yml.template + the compiled `.github/workflows/advance-lifecycle.yml`). So renaming it is NOT a mere local-var rename — it changes a machine-consumed output schema.

I renamed it to `specs[]` anyway, because (a) the task's acceptance grep flags the bare `prds` struct-field identifier (it excludes `prd:` but not `prds`), and the contract task's leak scan targets internal identifiers too (batch-4f retask: "scan widened to internal names"), and (b) I updated the coupled SOURCE consumers in lockstep: the two CI templates' `jq` (`.prds[]` → `.specs[]`) + `docs/ci/advance-loop.yml.template` + the coupled template tests. The `"prd:" + .slug` MATRIX LEGS were KEPT (the task grep excludes `prd:`; `prd:<slug>` is still an accepted CLI alias the contract task removes later).

**Touches / follow-up:** the compiled `.github/workflows/advance-lifecycle.yml` still reads `.prds[]` and must be REGENERATED (`install-ci`) by a human — otherwise propose-mode CI silently enumerates ZERO taskable specs (a `jq` reading a now-missing field returns empty, no loud error). This is the SAME compiled-workflow-regen follow-up the CLI-flag clean break already needs (D2). See "Manual follow-up" below.

### D2 — CLI flags: CLEAN BREAK (`--merge-prd`/`--propose-prd` → `--merge-spec`/`--propose-spec`, no alias)

Per the human decision in `prd-to-spec-4f-...` observation. The `mergePrd`/`proposePrd` fields (cli.ts + intake.ts `IntakeIntegrationFlags`) + the trigger-template `prd_flag`/`--*-prd` shell + `deriveIntakeFlags`'s `{prd}` result key all moved to `spec`. Verified: `dorfl intake 5 --merge-prd` now errors `unknown option '--merge-prd' (Did you mean --merge-spec?)`. Compiled `.github/workflows/intake.yml` still emits `prd_flag`/`--merge-prd` → must be regenerated by a human (it will call a now-rejected flag until then).

### D3 — verdict CONTENT keys `prdHumanOnly` / `prdNeedsAnswers` LEFT `prd`-spelled

The task's "Do NOT touch" list names `prdSlug`/`prdTitle`/`prdBody` verdict content keys (4d §4, command/data territory), and the acceptance grep excludes those three — but it OMITS `prdHumanOnly`/`prdNeedsAnswers`, which are the SAME class (read off the agent-emitted verdict JSON at `intake.ts:1894-1898`, declared on `IntakeVerdict` beside `prdSlug`/`prdTitle`/`prdBody`). I treated them as verdict-content-key survivors (the migration command / contract task owns the verdict-schema rename), consistent with the scope boundary's intent, even though they trip the literal grep. If the reviewer wants them swept, that is a coherent one-line follow-up but belongs with the rest of the verdict-schema keys, not split here.

### D4 — the `<prd>` protocol-template cluster LEFT `prd`-spelled

`wrapper(slug, prd, …)` / `buildAgentPrompt(…, task.prd, …)` (prompt.ts, do.ts, run.ts) + `ResolvedTask.prd` (spelled `prd:`, grep-excluded) + the `.replace(/<prd>/g, …)` substitution are COUPLED to the `<prd>` placeholder in the protocol doc `CLAIM-PROTOCOL.md` (`work/specs/ready/<prd>.md`), which the SEPARATE `rename-spec-protocol-contract-and-to-spec-skill` batch renames. Renaming the code `<prd>`→`<spec>` here would desync it from the still-`<prd>` protocol template and break substitution. A prior batch already recorded this same carve-out at prompt.ts (`ResolvedTask.prd … out of this batch's scope`). Left for the protocol-contract batch.

### D5 — `--prds-land-in` INPUT plumbing survivors LEFT (`cli.ts:730/734` `prdsRaw`/`return specs ?? prds`)

Explicitly out of scope per the task ("piece B does NOT touch the `prdsLandIn` input surface"). These locals mirror the `--prds-land-in` INPUT flag the contract task removes. Left.

### D6 — `to-prd`-shape intake prompt strings (intake.ts ~2388/2393) LEFT

Agent-prompt PROSE naming the `to-prd` skill + the `prd` artifact word + `work/specs/`/`do prd:`. These are the contract task's enumerated allow-list items ("sweep to `to-spec` if live prompt text, else allow-list") and the `to-prd`→`to-spec` skill rename is the protocol-contract batch's job. Domain-prose, not a code identifier. Left.

## What DID get purged (Piece B)

Exported `STAGED_PRDS_DIR`→`STAGED_SPECS_DIR`; interface method `RungExecutor.taskPrd`→`taskSpec` (+ the `'task-prd'`→`'task-spec'` TickRungKind value + `ANALYSE_RUNG_FOR_TYPE`); private consts `POOL_PRDS_DIR`/`PRD_PLACEMENT_SLOTS`→`POOL_SPECS_DIR`/`SPEC_PLACEMENT_SLOTS`, `close-job` `PRD_FOLDERS`+alias→`SPEC_LIFECYCLE_FOLDERS`/`SPEC_FOLDERS`; private fns `prdExists`→`specExists`, `heldPrdIsStale`→`heldSpecIsStale`, `resolvePrdSlug`→`resolveSpecSlug`, `buildTaskingPrd`→`buildTaskingSpec`, `close-job` `prdIssueNumber`→`specIssueNumber`; the `prds` struct field (LedgerSpecPool / RepoReport / MirrorPoolReport / SelectPriorityInput) + `SpecExistence.prdFile`/`prdTaskedFile` → `specFile`/`specTaskedFile` + `tasker-review-loop` `prdQuestions`→`specQuestions`; and ~all `prd*` local vars across advance-drivers/do-autopick/ledger-read/lifecycle-gather/mirror-pool-scan/needs-attention/prompt/scan/tasking/tasking-lock/intake/select-priority + coupled tests + the test helper `prdFile`/`opts.prds`.

## Manual follow-up (flagged for the human)

Regenerate the compiled `.github/workflows/*.yml` via `install-ci` (they are NOT edited here, exactly as `403a5be9` excluded compiled workflows). TWO reasons this run makes regen necessary: (1) `intake.yml` still emits the now-rejected `--merge-prd`/`--propose-prd` + `prd_flag`; (2) `advance-lifecycle.yml` still reads the renamed `scan --json` field as `.prds[]` (now `.specs[]`) → propose-mode CI would enumerate zero taskable specs until regenerated.
