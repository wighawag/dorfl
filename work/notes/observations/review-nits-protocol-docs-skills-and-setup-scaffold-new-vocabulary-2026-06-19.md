---
title: review-gate non-blocking nits for 'protocol-docs-skills-and-setup-scaffold-new-vocabulary' (Gate 2 approve)
date: 2026-06-19
status: open
reviewOf: protocol-docs-skills-and-setup-scaffold-new-vocabulary
---

## Non-blocking review findings

The PR/code review gate (Gate 2) APPROVED 'protocol-docs-skills-and-setup-scaffold-new-vocabulary' but raised the
following non-blocking findings (nits). They do not block integration; this
is their durable home for triage — promote-to-slice / keep / delete.

- Ratify the in-scope decision to RENAME the template files (slice-template.md -> task-template.md, prd-template.md -> brief-template.md) in both protocol copies, rather than keeping the old filenames. The slice said 'consider whether the template filenames themselves should rename... if so, do it in BOTH copies and update every reference; record the choice.' The agent did the rename (verified: both copies renamed, and the WORK-CONTRACT.md link at line 247 was repointed to task-template.md), which is the right call for vocabulary coherence - but it was NOT recorded in a ## Decisions block in the PR/commit body (the commit message is bare). Confirm the rename is what you want.
  (git show shows rename prd-template.md->brief-template.md and slice-template.md->task-template.md in both skills/setup/protocol/ and work/protocol/; the only in-doc reference (WORK-CONTRACT line 247) resolves correctly to task-template.md. The choice is correct and low-cost to reverse, but undocumented as a decision.)
- Ratify the decision to update the ADRs via dated FORWARD-NOTES (pointing old names to new) rather than authoring a NEW ADR for the regime-umbrella + per-regime-terminal decision. The PRD explicitly said 'A new ADR is likely warranted for the regime-umbrella decision and the per-regime terminal... the protocol-docs-skills-and-setup-scaffold-new-vocabulary slice is the natural place to capture it' - but it also said to ELICIT the durable why from the human, NEVER infer it. The agent correctly did NOT fabricate an ADR (no human was present to elicit from) and instead added honest forward-notes. This is defensible, but the PRD's 'new ADR likely warranted' intent is left open for you.
  (Three ADRs (command-surface-and-journeys, execution-substrate-decisions, methodology-and-skills) got 2026-06-19 forward-notes mapping old->new names without falsifying history. No new ADR was created. The PRD flagged an ADR as 'likely warranted' but conditioned it on eliciting the why from a human, which an autonomous build cannot honestly do.)
- The slice's last documentation criterion ('the done record / PR description calls out the ~/.agents/skills/ symlink re-points the user must make') is satisfied DURABLY in CONTEXT.md (line 80, which names the exact re-points), but the commit body itself is bare ('...; done') with no symlink callout. Confirm the CONTEXT.md home is sufficient, or add the callout to the PR description so it is visible at merge time.
  (CONTEXT.md line 80 records the symlink fact and the specific to-prd->to-brief / to-slices->to-task re-points the user owns. The commit message carries none of it. CONTEXT.md is arguably the better durable home, but the literal 'PR description / done record' channel is empty.)
- Two real but OUT-OF-SCOPE drift issues were correctly captured as observations rather than fixed here - please confirm the follow-ups are tracked: (1) skills/to-task/scripts/claim.sh still implements the superseded direct-main claim and references the now-removed work/backlog//work/in-progress/ folders; (2) sidecar.ts / surface-gate.ts still reference work/dropped/ and the promote-slice disposition value. Both are code/script concerns outside this docs+skills+setup slice's acceptance criteria; the agent recorded them as dated observations rather than silently leaving or scope-creeping them.
  (Observations work/notes/observations/claim-sh-still-describes-superseded-direct-main-claim-2026-06-19.md and sidecar-dropped-disposition-may-route-to-retired-flat-work-dropped-2026-06-19.md were added in this commit. claim.sh's staleness predates this slice (CLAIM-PROTOCOL.md already marked that mechanism superseded) and claim.sh is no longer an agent-runner runtime dependency per unify-claim.md, so neither is a regression introduced here; both warrant a follow-up task.)
