---
title: Land-time re-verify as one mode-agnostic primitive + parallel-merge CI shape + propose PR-merge-time safety (git-alone floor, host-accelerated ceiling)
slug: land-time-reverify-and-parallel-merge-ceiling
needsAnswers: true
---

> Launch snapshot — records intent at creation, NOT maintained. Current truth: `docs/adr/` (decisions) + the code; remaining work: `work/tasks/backlog/` slices. Originating design trail (the discussion that produced this brief): `work/notes/observations/ci-readme-claims-parallel-merge-thrashes-cas-but-integratelock-makes-it-safe-2026-06-21.md`, `work/notes/observations/integratelock-is-in-process-only-cross-ci-job-merge-relies-on-cas-retry-cap-2026-06-21.md`, `work/notes/observations/propose-mode-pr-merge-time-drift-runner-gates-push-time-tip-not-merge-time-tip-2026-06-21.md`. Engine already built: `integration-core.ts` (`freshWorktreeGate` default-on, `integrateLock`, `mergeRetries`), `integrator.ts` (the rebase + CAS-push land primitive, `NoneProvider`), `run.ts` (`createKeyedLock()` wiring). Affected doc: `docs/ci/README.md` (the advance-loop CI template). Verified findings folded in: `work/notes/findings/install-ci-can-set-branch-protection-via-api-but-needs-admin-token-not-ambient-github-token-2026-06-21.md` (resolves OPEN QUESTION 4) and `work/notes/findings/advance-surface-apply-rungs-can-carry-merge-questions-for-unmerged-branches-2026-06-21.md` (resolves OPEN QUESTION 2 — the runner becomes the merger via advance's surface/apply rungs).

## Problem Statement

Three related gaps sit on top of an integration engine that is ALREADY most of the way to the goal, but whose doctrine is unwritten, whose CI shape lags the engine, and whose propose path has a residual blind spot.

The shared insight underneath all three: **a clean `git` merge AND a human-approved diff both validate a change in the context it was AUTHORED, never the context it will LIVE.** Neither `git`'s clean-merge exit code nor a human reading a PR diff catches the merge that applies cleanly but is semantically broken (task A renames a symbol, task B adds a caller of the old name; both rebase clean; the merged tree fails to build). `blockedBy` only guards conflicts the author PREDICTED; semantic coupling is exactly the class you do not predict. The only proof a change is correct in the tree it lands in is **re-running acceptance on the post-rebase tree**.

1. **The land-time re-verify doctrine is unwritten, so it reads as accidental, not load-bearing.** The engine already does it (`freshWorktreeGate`, ON by default, gates the work branch REBASED onto current `<arbiter>/main` — the would-be-merged tree — for `verify` AND review), and a moved-`main` mid-push re-rebases + re-gates via the `mergeRetries` CAS loop. But the protocol docs (`WORK-CONTRACT.md`, `CLAIM-PROTOCOL.md`) never state the invariant, so "done/green" reads as the BUILD-time gate, and a future change could silently regress to trusting a clean rebase. The mode-agnostic primitive (`land = rebase onto current main + re-run verify + advance; a moved-main / lost-CAS INVALIDATES a prior green and re-arms verify`) is implemented but not named.

2. **The CI merge shape forbids parallelism the engine now makes safe.** `docs/ci/README.md` still mandates a SINGLE SEQUENTIAL merge job, justified by "parallel merge jobs would thrash the main-CAS." That justification predates `integrateLock` (serialises ONLY the land tail, keeping build/gate/review concurrent) + `mergeRetries` (turns a non-fast-forward push into re-rebase + re-gate + retry, never a thrash, never a `--force`). The `run` fleet ALREADY gets ultimate-parallelism-for-merge (parallel build, serialised land). CI does not — it leaves merge throughput on the table for a reason that no longer holds. Caveat: `integrateLock` is IN-PROCESS only; across separate CI jobs the CAS retry loop is the only cross-job serialiser, and its cap (`DEFAULT_MERGE_RETRIES = 5`) was sized for in-process siblings, not a wide matrix.

3. **Propose mode's re-verify covers the PR-PUSH tip, not the PR-MERGE-time tip.** The runner gates the rebased tip when IT integrates; for merge mode that moment IS the land (airtight). For propose mode the runner's job ends at "push branch + open PR" — a human (or auto-merge) clicks merge LATER, after `main` may have moved again, in a window the runner cannot re-gate because it is not the one merging. So propose's "verify ran on the merged tree" guarantee holds at push time, not merge time, and a human reviewing the diff has the same stale-base blind spot as a clean `git` merge.

The cost: a correct, hard-won safety property is undocumented and fragile; CI throughput is needlessly capped; and the DEFAULT integration mode (`propose`) has an unclosed drift window.

## Solution

Name the primitive once, then realise it as a git-alone FLOOR with a host-accelerated CEILING. Per the project steer: **git alone defines the correctness floor (it must be safe with nothing but `git push` + ref CAS against a bare `--bare` arbiter and `NoneProvider`); a capable host can only RAISE the ceiling, never be required for safety. GitHub is the benchmark — if the design degrades gracefully from GitHub down to bare git, any middling host (GitLab, Gitea, plain SSH) slots in between.**

- **One mode-agnostic landing primitive, written into the protocol.** Document the invariant the engine already implements: every land = fetch current `main` → rebase → re-run `verify` (and review) on the rebased tree → advance; a lost CAS / moved-`main` INVALIDATES any prior green and re-arms the gate. Merge mode runs it inline at the sequential/serialised land; propose mode runs it at the human checkpoint. Human review is ADDITIVE (intent/design/security), NEVER a substitute for the re-verify. Capture this as durable rationale in an ADR and as an invariant line in `WORK-CONTRACT.md` / `CLAIM-PROTOCOL.md` (edit `skills/setup/protocol/` SOURCE + mirror into `work/protocol/`, byte-identical per this repo's protocol-docs rule).

- **CI parallel-merge shape, sized for cross-job landing.** Update `docs/ci/README.md` (and the validated `advance-ci-template.ts`) so merge mode CAN fan out, with the cross-job land serialised by whichever mechanism the open questions resolve (a scaled CAS retry cap, and/or a cross-job concurrency group / ref-lock so losers QUEUE rather than bounce to needs-attention). Record explicitly: across runners the CAS loop IS the queue; within a runner the in-memory `integrateLock` is the optimisation.

- **Propose PR-merge-time safety, two-tier.** Floor (git-alone / bare host): the gap closes only when the RUNNER performs the merge (its `freshWorktreeGate` then covers the merge-time tip); a human-merged PR on a bare arbiter is gated at push time only — documented as a known limitation with the mitigation "let the runner merge" or "re-run verify after rebase before merging." Ceiling (GitHub benchmark): branch protection "require branches up to date before merging" + a required `verify` status check forces a rebase + re-verify against current `main` before the merge button works (tier 1); a merge queue (`merge_group` trigger) does speculative-rebase composition checking and removes rebase churn (tier 2). A capable-but-not-GitHub host slots between by whatever subset it supports.

From the user's perspective: the safety property is now a stated, testable contract rather than an emergent accident; merge mode in CI is as parallel as the engine allows without ever landing a semantically-broken merge; and a propose PR cannot be merged stale without a re-verify wherever the host (or the runner-as-merger) can enforce it, with an honest documented floor where it cannot.

## User Stories

1. As a maintainer, I want the protocol to STATE the land-time re-verify invariant (rebase onto current main → re-run verify → advance; a moved-main invalidates a prior green), so the safety the engine already provides is a named contract, not an accident a refactor can silently drop.
2. As a maintainer, I want ONE mode-agnostic landing primitive in the docs, with merge and propose described as two FRONTENDS to it (runner-inline vs human-checkpoint), so propose-vs-merge is understood as "is a human approval required before the land," not "is the build parallel" or "is the tree re-verified."
3. As a maintainer, I want it written that human review is ADDITIVE and never replaces the re-verify, so nobody treats "a human approved the PR" as proof the merged tree builds.
4. As a maintainer running CI, I want merge mode to fan out in parallel (parallel build/gate/review, serialised land), so merge throughput matches the `run` fleet's instead of being capped by a stale single-sequential-job rule.
5. As the runner across separate CI jobs, I want concurrent merge landings to QUEUE deterministically (not bounce to needs-attention under a burst), since the in-memory `integrateLock` does not span jobs and only the CAS loop does — so the cross-job serialiser is sized/shaped for the matrix width on purpose. (SEE OPEN QUESTION 1.)
6. As `docs/ci/README.md`, I want my "parallel merge thrashes the CAS" claim corrected to reflect `integrateLock` + `mergeRetries`, so the doc stops contradicting the engine.
7. As a maintainer on GitHub, I want a propose PR to be UN-MERGEABLE while stale: branch protection "require up to date" + a required `verify` check forces a rebase + re-verify against current `main` before the merge button works, so the PR-merge-time drift window closes (tier 1).
8. As a maintainer on GitHub at scale, I want an optional merge queue (`merge_group` trigger on the verify workflow) that speculatively rebases + verifies + merges in order, so two individually-green PRs that break TOGETHER are caught and the rebase churn of tier 1 is removed (tier 2). (SEE OPEN QUESTION 3.)
9. As a maintainer on a bare / no-host / less-capable-host repo, I want the runner ITSELF to close the merge-time gap (not just document it): the advance loop SURFACES unmerged `work/*` branches / open PRs as MERGE-QUESTIONS, my ANSWER is the approval, and the apply step LANDS an answered-merge through the re-verify primitive — so the gap closes by the runner being the merger, with or without a host PR surface. (RESOLVES OPEN QUESTION 2 — see the surface/apply finding + the dedicated stories below; the human-merges-a-stale-PR limitation only remains if a maintainer DELIBERATELY merges outside the runner.)
10. As a maintainer, I want the host tiers framed as RAISING a ceiling over a safe git-alone floor (never as a safety prerequisite), so the tool stays honest on bare git and merely BETTER on GitHub — GitHub as the benchmark that proves the gradient.
11. As a maintainer adopting the GitHub ceiling, I want `install-ci` to AUTO-CONFIGURE Tier 1 (the required `verify` check + `strict: true` = require-up-to-date, via one `gh api` branch-protection / ruleset call) WHEN it is run with an admin-scoped credential, and to fall back to printing the exact ready-to-run command + manual instructions when it has only the ambient `GITHUB_TOKEN` — so the gate is reliably present where it can be, and never silently skipped where it cannot. (RESOLVED — see the verified finding + Implementation Decisions; the same call also creates the Tier-2 `merge_queue` ruleset rule.)
12. As a maintainer, I want a test that proves the land-time re-verify actually catches a clean-rebase-but-semantically-broken merge (A renames a symbol, B adds a caller; both rebase clean; the merged tree fails `verify`) — so the invariant is enforced by a test, not just asserted in prose.
13. As a maintainer, I want a test that proves concurrent merge landings serialise correctly (one lands, the other re-rebases + re-gates + lands or routes; never both-land-broken by timing; never a `--force` to main), at both in-process (`integrateLock`) and cross-job (CAS-loop) levels.
14. As the advance loop, I want a SECOND surfacer that enumerates unmerged `work/*` branches (reachability against `main` — the git-alone floor) and, where a host exists, open PRs (`gh pr list` — the ceiling rendering), and surface each as a MERGE-QUESTION into the SAME sidecar shape with a `merge|hold|drop` disposition — so propose-mode landing flows through the existing surface->answer->apply machinery, not a bespoke verb. (The judgement `surface-questions` skill is untouched; this is a state-sourced surfacer feeding the same sidecar.)
15. As the apply step, I want an answered `merge` disposition to invoke the LAND primitive (rebase onto current main -> re-run verify on the rebased tip -> advance), exactly as an answered `promote-slice`/`dropped` disposition triggers its `git mv` today — so "answer drives a runner action" reuses the existing disposition-dispatch pattern, now extended to the land action.
16. As a maintainer, I want an answered-merge to be a CONDITIONAL request, not a guarantee: if `main` moved between my answer and apply and the rebased tip FAILS `verify`, apply must REFUSE to merge (route to needs-attention or re-surface), never land a clean-rebase-but-broken tree — so my approval means "merge IF it still verifies on current main," and the clean-rebase-but-broken case is caught at the last moment. (Sub-decision in OPEN QUESTION 6: does a CHANGED merge-base re-surface even on a GREEN re-verify?)
17. As a maintainer, I want MERGE-QUESTIONS gated by their OWN config axis, SEPARATE from `observationTriage` and defaulting HIGHER, because "should this built work merge?" is materially more consequential than "is this observation worth promoting?" — an observation question defaults `off` (safely ignorable), but a merge-question silently dropped means finished, pushed work never lands. So merge-questions must NOT ride the observation gate. (SEE OPEN QUESTION 7 for the exact gate name + default.)

### Autonomy notes (the two gate axes)

- **`humanOnly` (slicing):** NOT set. This brief is agent-sliceable once its open questions are answered — it is a design + docs + CI-template + test effort, not a secrets/release/security decision.
- **`needsAnswers`: TRUE.** There are genuine unresolved design decisions (below) that must NOT be guessed — wrong guesses here ship a stale-base merge or spurious needs-attention bounces. The auto-slicer must refuse until these are answered and the flag cleared.

Open questions (resolve before slicing):

1. **Cross-job merge serialiser.** For parallel merge in CI, is the cross-job mechanism (a) just a SCALED `mergeRetries` cap sized to expected matrix width (simple, but a wide burst still risks needs-attention bounces past the cap), (b) a cross-job concurrency group / ref-based land-lock so losers QUEUE rather than retry-then-bounce, or (c) GitHub Actions `concurrency:` on the merge job (host-specific, violates the git-alone-floor framing for the merge serialiser itself)? Likely (a) as the git-alone floor + (b) or (c) as an optional accelerator — confirm.
2. **Runner-merges-propose.** RESOLVED (verified finding `work/notes/findings/advance-surface-apply-rungs-can-carry-merge-questions-for-unmerged-branches-2026-06-21.md`): the runner becomes the merger via advance's EXISTING surface->answer->apply rungs, NOT a bespoke `land`/`merge-pr` verb. A SECOND surfacer enumerates unmerged `work/*` branches (reachability floor) + open PRs (`gh pr list` ceiling) and surfaces each as a MERGE-QUESTION (sidecar shape, `merge|hold|drop` disposition); the answer IS the human approval; apply dispatches an answered `merge` through the LAND primitive (rebase -> re-verify -> advance), reusing the `promote-slice`/`dropped` disposition-dispatch pattern. The human-approval-required nature of propose is naturally expressed: propose surfaces the question (answer = approval), merge does not (auto-land); both share the identical apply-time land. (Remaining sub-decisions split out to OPEN QUESTIONS 6 and 7.)
3. **Merge queue scope.** Is tier-2 (GitHub Merge Queue, `merge_group` trigger) IN scope for this brief, or captured as a follow-on once tier-1 (up-to-date + required check) ships? Tier 1 closes the drift window; tier 2 adds composition-checking + removes churn but is a larger CI-template change.
4. **Branch-protection provisioning.** RESOLVED (verified finding `work/notes/findings/install-ci-can-set-branch-protection-via-api-but-needs-admin-token-not-ambient-github-token-2026-06-21.md`): Tier 1 IS API-settable (`PUT .../branches/{branch}/protection` with `required_status_checks: { strict: true, checks: [{ context: "verify" }] }`, where `strict: true` IS verbatim "require branches up to date before merging"; or the equivalent ruleset, which also carries the `merge_queue` rule for Tier 2). BUT it needs repository **Administration: write** — the ambient CI `GITHUB_TOKEN` is NEVER admin (confirmed), the SAME wall as the `Allow Actions to create PRs` toggle, except this one IS automatable with the right token. DECISION: `install-ci` AUTO-CONFIGURES when run with an admin-scoped credential (a human admin's `gh` at setup, or a `runner-in-ci`-provisioned PAT / GitHub App with `Administration: write`), guaranteeing the `context` matches the workflow it emits, and ORDERING the step (run the check once, then require — or use a ruleset `do_not_enforce_on_create`) so a never-run required check does not deadlock all merges; otherwise it PRINTS the exact `gh api` command + the manual fallback. Wizard/token provisioning is `runner-in-ci`'s; this brief defines WHAT it must provision.
5. **`origin/main` reconcile = merge vs rebase for the human.** `CLAIM-PROTOCOL.md` already tells the human-as-participant to reach main via `pull --rebase` then push. A plain `git pull` (merge) does NOT re-run verify on the reconciled tree (observed live during this brief's own session). Should the docs additionally WARN that a human's local reconcile must be a rebase + a manual `verify`, or is that out of scope (the human path is explicitly lower-assurance than the runner path)?
6. **Stale-approval policy for an answered merge-question.** An answered-merge is conditional: apply re-verifies on the rebased tip and REFUSES on red (decided, story #16). Open: when `main` moved between answer and apply but the rebased tip STILL verifies GREEN, does apply (a) honour the prior approval and land (cheap; trusts that a green re-verify is sufficient), or (b) RE-SURFACE the question because the merge-base CHANGED (conservative; the host-agnostic analogue of GitHub's "dismiss stale approvals when the base changes")? Likely (a) by default with (b) as an opt-in strictness, but confirm — it sets how often a human re-answers.
7. **Merge-question gate (name + default).** Merge-questions need their OWN gate, distinct from `observationTriage` (story #17). Decide: (a) the gate name (e.g. `mergeQuestions` / `surfaceMerge`), resolved through the SAME precedence chain as the other gates (flag > env > per-repo > global > default); (b) its default — `observationTriage` defaults `off`, but a merge-question dropped means pushed work never lands, so the default should be HIGHER (likely `ask` — surface + wait for a human answer; possibly `auto` for a trusted repo = auto-land an answered/unblocked merge, the merge-mode-like fast path; `off` only for a repo that lands by some other means); (c) whether it is one 3-state gate (`off|ask|auto`, mirroring `observationTriage`'s shape) or a boolean. Do NOT collapse it into `observationTriage`.

## Implementation Decisions

Decisions already fixed at launch (the engine exists; these constrain the slices):

- **Do NOT re-architect the land primitive — it is built.** `integration-core.ts`'s `performIntegration` already implements rebase → fresh-worktree gate (verify + relocated review) → integrate, with the `mergeRetries` re-rebase-and-retry loop. The work is NAMING it (docs/ADR), EXTENDING the CI shape to use it, CLOSING the propose merge-time tier, and TESTING the invariant — not rebuilding the engine.
- **Protocol-doc edits are dual-write.** Per this repo's rule, edit `skills/setup/protocol/*` (the SOURCE OF TRUTH) and mirror byte-identically into `work/protocol/*`; `diff -r` must be clean.
- **The CI template is validated code.** `docs/ci/README.md`'s shape is enforced by `src/advance-ci-template.ts` + `test/advance-ci-template.test.ts`; the parallel-merge change touches BOTH the doc and that emitter/test, not the doc alone.
- **Provider stays purely arbiter-derived.** The GitHub-ceiling work layers on top of the existing `selectProvider` (GitHub URL ⇒ `gh`, else `none`); it does NOT add a provider override axis. The merge-queue/branch-protection config is a SETUP concern (`runner-in-ci`), not a new integration provider.
- **Tier-1 GitHub provisioning is auto-when-admin, instruct-otherwise (RESOLVED, OPEN QUESTION 4).** Verified: `PUT /repos/{owner}/{repo}/branches/{branch}/protection` with `required_status_checks: { strict: true, checks: [{ context: "verify" }] }` sets BOTH the required `verify` check AND "require branches up to date before merging" (`strict: true` IS that toggle); the equivalent ruleset (`POST .../rulesets`) also carries the Tier-2 `merge_queue` rule. This needs repository **Administration: write** — the ambient CI `GITHUB_TOKEN` is NEVER admin (the same wall as the `Allow Actions to create PRs` toggle, but THIS one is API-automatable with an admin token). So `install-ci`: (a) AUTO-CONFIGURES when run with an admin-scoped credential, generating the `context` to MATCH the workflow it emits; (b) ORDERS it (run the check once before requiring it, or use a ruleset `do_not_enforce_on_create`) so a never-run required check cannot deadlock all merges; (c) otherwise PRINTS the exact `gh api` command + manual fallback. The credential/wizard is `runner-in-ci`'s concern; this brief fixes the BEHAVIOUR (auto-vs-instruct + the ordering guard).
- **Never `--force` to main; never auto-resolve a conflict.** Unchanged invariants (ADR §10); every new path preserves them.
- **Propose landing flows through advance's surface/apply rungs, not a bespoke verb (RESOLVED, OPEN QUESTION 2).** Verified the rungs exist and that an answered disposition ALREADY dispatches a runner action (`triage-persist.ts`: `promote-slice`/`dropped` -> `git mv`). So: a SECOND, STATE-sourced surfacer (enumerate `work/*` branches unreachable from `main`; + `gh pr list` where a host exists) emits MERGE-QUESTIONS into the SAME sidecar shape with a `merge|hold|drop` disposition; the `surface-questions` JUDGEMENT skill is UNTOUCHED. The apply step gains a `merge`-disposition dispatch that invokes the EXISTING land primitive (`integration-core.ts` rebase -> re-verify -> advance), mirroring the existing disposition-dispatch. An answered-merge is CONDITIONAL: apply re-verifies the rebased tip and refuses (needs-attention / re-surface) on red — never lands a clean-rebase-but-broken tree. PR-OPTIONAL by construction (branch reachability is the floor; PRs are the ceiling rendering), so it works on a bare `--bare` arbiter.
- **Merge-questions get a SEPARATE, higher-default gate from `observationTriage` (fixed; tuning in OPEN QUESTION 7).** Decided: merge-questions must NOT ride `observationTriage` (default `off`), because a dropped merge-question means pushed work never lands — strictly more consequential than a dropped observation-promote prompt. It is its own per-repo gate resolved through the SAME precedence chain (flag > env > per-repo > global > default), defaulting HIGHER than `off`. The exact name (`mergeQuestions`/`surfaceMerge`), default (`ask` vs `auto`), and shape (3-state `off|ask|auto` vs boolean) are OPEN QUESTION 7; the SEPARATION + higher-default is fixed.

> Trimmed at slice-time: the per-slice seams (which doc lines, which template function, which test fixtures) move into the tasks; the durable WHY (the authored-context-vs-lived-context principle, the floor/ceiling gradient) goes into an ADR (working name `land-is-rebase-reverify-advance-one-primitive-two-frontends`).

> NOTE on `needsAnswers`: OPEN QUESTIONS 2 and 4 are now RESOLVED (folded in above). The flag STAYS `true` because questions 1 (cross-job merge serialiser), 3 (merge-queue in-scope-now vs follow-on), 5 (human reconcile rebase-vs-merge warning), 6 (stale-approval policy: green-re-verify-is-enough vs re-surface-on-base-change), and 7 (merge-question gate name + default) remain open — clear those five, then drop the flag.

## Testing Decisions

- **The load-bearing test is the clean-rebase-but-broken-merge.** Two work branches, disjoint files, no textual conflict: A renames a symbol, B adds a caller of the old name. Both rebase clean onto each other. Assert the SECOND to land fails `verify` on the rebased tip and routes to needs-attention (or, with the host tier, is blocked from merging) — i.e. the clean rebase is NOT trusted. This is the test that proves the whole thesis; prior art: the existing `integration-core` fresh-worktree-gate tests + `advance-ci-template.test.ts`.
- **Concurrency tests at two levels.** In-process: two same-repo merge jobs through `run` with the `integrateLock` wired — one lands, the loser re-rebases + re-gates + lands or routes; never both-land-broken. Cross-job: simulate separate processes racing the same arbiter ref (the CAS loop alone) — assert deterministic convergence within the (resolved) cap, and the chosen queue/bounce behaviour past it.
- **Test external behaviour, not internals:** assert on what lands on `main`, what routes to needs-attention, and that `verify` ran on the rebased tree — not on which private function was called.

> Also trimmed at slice-time (moves into the tasks' acceptance criteria / the ADR).

## Out of Scope

- **Re-architecting the integration engine.** The land primitive, `freshWorktreeGate`, `integrateLock`, and `mergeRetries` already exist; this brief documents, extends to CI, closes the propose tier, and tests them — it does not rebuild them.
- **A bespoke cross-host merge-queue implementation.** Beyond the git-alone CAS-loop floor and the GitHub merge-queue ceiling, building a provider-agnostic speculative-merge queue from scratch is out (revisit if a non-GitHub host with such a primitive becomes a target).
- **Changing `blockedBy` / `sliceAfter` semantics.** Dependency resolution is unchanged; this brief is about the LAND step, not eligibility.
- **The human-path lower-assurance reconcile**, unless OPEN QUESTION 5 pulls it in — the runner path is the assured path; the human-as-participant path is explicitly allowed to be lighter.
- **The `runner-in-ci` `install-ci` CLI surface itself**, owned by `work/prd/runner-in-ci.md`; this brief may DEFINE what that setup must provision for the GitHub ceiling (branch protection / required check / merge queue) but does not build the wizard.

## Further Notes

- This brief was produced from a live discussion that started as "should merge mode allow parallel tasks?" and converged — via the observations linked in the snapshot — on "the engine already does the right thing; name it, extend it to CI, close the propose tier." Read those three observation notes first; they carry the reasoning compressed here.
- A live illustration occurred DURING the session: a `git push` was rejected non-fast-forward, a `git pull` reconciled via a MERGE (not a rebase), and that merge did not re-run `verify` — the exact "git said clean, verify did not run" shape this brief exists to make impossible on the assured path. (Disjoint ledger files, so harmless that time — which is precisely why the failure mode is easy to miss.)
- North star to keep in every slice: git-alone is the floor that must be safe; the host raises the ceiling; GitHub is the benchmark that proves the gradient down to bare git.
