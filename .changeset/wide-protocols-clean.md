---
"dorfl": patch
---

WORK-CONTRACT: reword the `release-lock --entry <literal>` note to speak in the present. Drop the historical "pre-vocabulary-cutover `slice-<slug>` / `prd-<slug>` prefix" reference and describe the case generically (e.g. a lock entry left un-derivable after a rename).
